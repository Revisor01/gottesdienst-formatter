---
phase: quick-260617-u0x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/formatting.py
  - web/main/routes.py
  - web/churchdesk_api.py
  - web/templates/churchdesk_events.html
  - web/migrations/versions/c3d4e5f6a7b8_fix_single_church_burg_marne.py
  - web/tests/test_formatting.py
  - web/tests/test_routes_defensive.py
autonomous: true
requirements:
  - JULI-01-none-output
  - JULI-02-single-church
  - JULI-03-serverfehler-bearbeiten
  - JULI-04-vikar-titel
  - JULI-05-propst-titel
  - JULI-06-ort-fehlt-markieren

must_haves:
  truths:
    - "Leere/None-Felder erscheinen im Output als nichts — nie als String 'None'"
    - "Klick auf 'bearbeiten'/Export erzeugt keinen Serverfehler mehr, auch bei Events mit fehlenden Feldern"
    - "Vikar:innen werden als 'Vik. Nachname' ausgegeben, Vorname entfernt"
    - "Pröpst:innen werden als 'Pr. Nachname' (Vorname entfernt) ausgegeben"
    - "Single-Church-Orte (Burg, Marne) erscheinen als 'Ort, Kirche', nicht mit konkretem Kirchennamen"
    - "Büsum St. Clemens / ohne Kirche erscheint als 'Büsum' (unverändert), Perlebucht-Sonderfall bleibt"
    - "Events ohne Ort sind rot markiert und NICHT vorausgewählt, bleiben aber in der Liste"
  artifacts:
    - path: "web/formatting.py"
      provides: "None-/'None'-Abfang in format_pastor + Vik./Pr.-prefix_map"
      contains: "Vikarin"
    - path: "web/main/routes.py"
      provides: "Defensive None-Normalisierung + .get()-Zugriffe im Export-/Excel-Pfad"
      contains: ".get("
    - path: "web/migrations/versions/c3d4e5f6a7b8_fix_single_church_burg_marne.py"
      provides: "Idempotente DB-Korrektur: Burg/Marne aus multi_church entfernen"
      contains: "down_revision = 'b2c3d4e5f6a7'"
    - path: "web/tests/test_routes_defensive.py"
      provides: "Tests fuer None-Haerten im Export-/Excel-Pfad"
      contains: "convert_churchdesk_events_to_boyens"
  key_links:
    - from: "web/main/routes.py:convert_churchdesk_events_to_boyens"
      to: "web/formatting.py:format_pastor"
      via: "normalisierter contributor (None -> '')"
      pattern: "format_pastor\\("
    - from: "web/churchdesk_api.py:_DEFAULT_MULTI_CHURCH_CITIES"
      to: "web/migrations c3d4e5f6a7b8"
      via: "Burg/Marne aus Default-Liste UND DB-Seed entfernt"
      pattern: "multi_church"
---

<objective>
Behebt sechs Redaktions-Feedbacks (Juli) am Gottesdienst-Formatter:
1. "None" im Output bei leeren Feldern → leere Ausgabe.
2. Single-Church-Orte (Burg, Marne) sollen "Ort, Kirche" statt konkretem Kirchennamen zeigen.
3. Serverfehler beim Bearbeiten/Exportieren von Events mit fehlenden Feldern → defensiv abfangen.
4. Vikar:innen → "Vik. Nachname".
5. Pröpst:innen → "Pr. Nachname".
6. Events ohne Ort: rot markiert + nicht vorausgewählt (drinlassen) — Härtung/Verifikation.

Purpose: Der Boyens-Output muss 1:1 übernehmbar sein — "None"-Strings und Serverfehler blockieren die redaktionelle Nutzung.
Output: Gehärtete Formatierungs- und Export-Pfade, korrigierte Location-Defaults + idempotente DB-Migration, erweiterte Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- Verifizierte Fakten aus Code- und API-Analyse. Executor braucht keine erneute Exploration. -->

VERIFIZIERTE BEFUNDE (curl gegen ChurchDesk Org 2729, Juli 2026):
- `contributor` ist real `None` (Python None) bei vielen Events, z.B. "Willkommen in der Kirchenkiste".
- `location`/`locationName` sind real `None` bei manchen Events.
- format_pastor(None) -> ''  (ok). ABER format_pastor("None") -> "None"  (BUG — String "None" muss als leer behandelt werden).
- extract_boyens_location(None, ...) -> ''  (ok, verträgt None).
- datetime.fromisoformat(None) -> TypeError  (Serverfehler-Quelle wenn startDate fehlt).
- event['title'] / event['location'] / event['contributor'] / event['parishes'] mit FEHLENDEM Key -> KeyError (Serverfehler-Quelle im Export-Pfad, routes.py Zeile ~167-171).

format_pastor (web/formatting.py): if not contributor: return "". str(contributor).strip().
  DB-Lookup (_lookup_pastor) zuerst, dann _regex_fallback_pastor mit prefix_map.
  prefix_map (Zeile ~232): Liste von (prefix, title)-Tupeln, längere/spezifischere Prefixe zuerst.
  Vikar/Vikarin/Propst/Pröpstin sind aktuell NICHT in prefix_map -> werden durchgereicht (verifiziert).

extract_boyens_location (web/churchdesk_api.py):
  _DEFAULT_MULTI_CHURCH_CITIES = ['heide', 'brunsbüttel', 'büsum', 'burg', 'marne']  (Zeile 226)
  -> 'wrohm' und 'pahlen' sind NICHT in der Liste (bereits Single-Church, KEINE Änderung nötig).
  -> Nur 'burg' und 'marne' müssen raus (Büsum bleibt wegen Perlebucht-/St.Clemens-Sonderlogik MULTI).
  Büsum-Sonderfall (Zeile ~421): St. Clemens -> 'Büsum', Perlebucht -> 'Büsum, Perlebucht'. BLEIBT.
  Multi-Kirchen-Orte werden in DB-Tabelle location_rules (kind='multi_church') gespiegelt.
  load_location_rules: DB überschreibt Defaults NUR wenn Tabelle Regeln enthält.

Migration-Mechanik:
  entrypoint.sh führt `flask db upgrade` beim Container-Start aus (automatisch).
  Aktueller Migrations-Head: b2c3d4e5f6a7 (add_location_rules, seedet multi_church inkl. 'burg','marne').
  Neue Migration MUSS down_revision = 'b2c3d4e5f6a7' setzen. Daten-Korrektur muss IDEMPOTENT sein.
  Seed-Migration b2c3d4e5f6a7 nutzt op.bulk_insert; neue Migration nutzt op.execute(DELETE ... WHERE).

Test-Setup:
  pytest: pyproject.toml [tool.pytest.ini_options] pythonpath=["."], testpaths=["tests"].
  conftest.py setzt formatting._pastor_cache mit Fixtures (autouse). Für Vik./Pr.-Tests OHNE DB-Match:
  Cache enthält die Vikar:innen-Nachnamen NICHT -> regex_fallback greift -> Titel kommt aus prefix_map.
  Test-Runner: `cd web && python -m pytest tests/ -q`

String-Formatierung: IMMER .format(), NIE f-strings (CLAUDE.md-Konvention).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: None-Normalisierung + Serverfehler-Härtung (Problem 1 & 3)</name>
  <files>web/formatting.py, web/main/routes.py, web/templates/churchdesk_events.html, web/tests/test_routes_defensive.py</files>
  <behavior>
    - format_pastor("None") == "" und format_pastor("none") == "" und format_pastor(None) == "" und format_pastor("NONE") == ""
    - format_pastor("Pastor Mueller") bleibt unverändert "P. Mueller" (Regression).
    - convert_churchdesk_events_to_boyens([event mit allen Feldern None und gültigem startDate]) wirft KEINE Exception und liefert eine Datums-Zeile ohne "None".
    - convert_churchdesk_events_to_boyens([event OHNE 'title'/'location'/'contributor'/'parishes'-Keys]) wirft KEINE Exception (KeyError-frei).
    - convert_churchdesk_events_to_boyens([event mit fehlendem/leerem startDate]) wirft KEINE Exception (Event wird übersprungen, nicht crashen).
    - process_excel_file-Pfad: eine Zeile mit NaN-Titel/Mitwirkendem/Ort erzeugt kein "None" im Output (über _build_location_entries-Ergebnis prüfbar oder Unit auf den Sammel-Loop).
  </behavior>
  <action>
    (a) formatting.py — format_pastor defensiv härten: Direkt nach `name = str(contributor).strip()` (bzw. vor dem `if not contributor`-Check ergänzen) abfangen, wenn der String case-insensitiv "none" ist → "" zurückgeben. Konkret: nach dem bestehenden `if not contributor: return ""` eine Prüfung `if str(contributor).strip().lower() == "none": return ""` einfügen. NUR exakt "none" abfangen (nicht Teilstrings wie "Nonenberg").
    (b) routes.py — convert_churchdesk_events_to_boyens: Beim Aufbau des Event-Dicts (Zeile ~165-172) alle Feldzugriffe auf `.get(key)` umstellen und None normalisieren: title via `event.get('title') or ''`, location via `event.get('location') or ''`, contributor via `event.get('contributor') or ''`, parishes via `event.get('parishes') or []`. startDate-Parsing absichern: `raw_start = event.get('startDate')`; wenn leer/None → `continue` (Event überspringen, nicht crashen); `datetime.fromisoformat` nur auf nicht-leeren String anwenden, in try/except (bei Parse-Fehler `continue`).
    (c) routes.py — process_excel_file: `str(raw_ort)` macht aus None den String "None". Vor dem extract_boyens_location-Aufruf raw_ort-None abfangen: nur `str(raw_ort)` wenn raw_ort nicht NaN/None, sonst "". (Der Titel-/Mitwirkender-Zweig nutzt bereits `not pd.isna(...)`-Guards — prüfen dass raw_ort denselben Schutz hat: aktuell `row['Standortnamen'] if not pd.isna(...) else row['Gemeinden']` — wenn AUCH Gemeinden NaN ist, bleibt NaN → `str(NaN)`='nan'. Daher: `location_raw = raw_ort if (raw_ort is not None and not pd.isna(raw_ort)) else ''; location = extract_boyens_location(str(location_raw) if location_raw else '', for_export=True)`.)
    (d) churchdesk_events.html — Jinja-Templates härten, damit kein "None" gerendert wird: bei den Input-`value`-Attributen `event.title`, `event.location`, `event.contributor` jeweils `or ''` ergänzen (z.B. `value="{{ event.title or '' }}"`). Gilt für die drei edit-Input-Felder (Zeile ~111, ~115, ~119).
    (e) Tests: Neue Datei web/tests/test_routes_defensive.py anlegen. Importiert convert_churchdesk_events_to_boyens und process_excel_file aus main.routes. Deckt die Behavior-Fälle oben ab. WICHTIG: main.routes importiert flask — Tests laufen im venv (flask installiert). Für die None-format_pastor-Fälle die Tests in test_formatting.py ergänzen (kein flask-Import nötig).
    String-Formatierung durchgehend .format(), keine f-strings.
  </action>
  <verify>
    <automated>cd web && python -m pytest tests/test_formatting.py tests/test_routes_defensive.py -q</automated>
  </verify>
  <done>format_pastor("None")/(None) → ""; Export-/Excel-Pfad wirft bei None/fehlenden Feldern keine Exception und produziert kein "None"; Template rendert leeres value statt "None"; neue Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vikar:in → "Vik.", Pröpst:in → "Pr." (Problem 4 & 5)</name>
  <files>web/formatting.py, web/tests/test_formatting.py</files>
  <behavior>
    - format_pastor("Vikarin Laura Karlsen") == "Vik. Karlsen"
    - format_pastor("Vikar Max Mustermann") == "Vik. Mustermann"
    - format_pastor("Propst Dr. Andreas Crystall") == "Pr. Dr. Crystall"  (Vorname Andreas raus, Dr. bleibt — konsistent mit _extract_surname)
    - format_pastor("Pröpstin Anna Beispiel") == "Pr. Beispiel"
    - Regression: format_pastor("Pastorin Schmidt") == "Pn. Schmidt" (Pastorin darf NICHT von einem zu kurzen Prefix gekapert werden).
  </behavior>
  <action>
    formatting.py — _regex_fallback_pastor prefix_map (Zeile ~232) ergänzen. WICHTIG: Reihenfolge — spezifischere/längere Prefixe zuerst, weibliche Form vor männlicher (sonst matcht "Vikar " auf "Vikarin "). Einträge hinzufügen:
      ('Pröpstin ', 'Pr.'), ('Propst ', 'Pr.'), ('Vikarin ', 'Vik.'), ('Vikar ', 'Vik.').
    Platzierung: vor den generischen ('Pastorin ','Pn.')/('Pastor ','P.')-Einträgen ist unkritisch, da kein Prefix-Präfix-Konflikt besteht (Propst/Vikar teilen keinen Anfang mit Pastor). Aber 'Vikarin ' MUSS vor 'Vikar ' und 'Pröpstin ' vor 'Propst ' stehen.
    Annahme dokumentieren (Simon-Entscheidung): Propst → "Pr." mit Vorname entfernt; Dr. bleibt erhalten (Standard via _extract_surname → "Pr. Dr. Crystall"). Falls Redaktion später "Pr. Crystall" ohne Dr. will, ist das eine separate _extract_surname-Anpassung.
    Hinweis zu "Laura Karlsen" OHNE Titel: kommt die Vikarin in der API ohne "Vikarin"-Prefix an (nur "Vorname Nachname"), greift prefix_map NICHT — dann muss ein Pastor-DB-Eintrag (last_name='Karlsen', title='Vik.') über die Admin-Pastor-Oberfläche angelegt werden. Das ist ein Daten-Pflege-Schritt, KEINE Code-Änderung — im SUMMARY als Hinweis für Simon notieren.
    String-Formatierung .format(), keine f-strings.
  </action>
  <verify>
    <automated>cd web && python -m pytest tests/test_formatting.py -q -k "vikar or propst or pastor"</automated>
  </verify>
  <done>Vik./Pr.-Prefixe werden korrekt abgekürzt, Vorname entfernt; Pastorin/Pastor-Regression grün; neue parametrisierte Testfälle in test_formatting.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single-Church-Orte Burg/Marne + idempotente DB-Migration + Ort-fehlt-Härtung (Problem 2 & 6)</name>
  <files>web/churchdesk_api.py, web/migrations/versions/c3d4e5f6a7b8_fix_single_church_burg_marne.py, web/tests/test_formatting.py</files>
  <behavior>
    - extract_boyens_location("Burg Kirche", for_export=True) == "Burg, Kirche"
    - extract_boyens_location("Burg", for_export=True) == "Burg, Kirche"
    - extract_boyens_location("Marne Kirche", for_export=True) == "Marne, Kirche"
    - extract_boyens_location("Marne", for_export=True) == "Marne, Kirche"
    - Regression Büsum: extract_boyens_location("Büsum - St. Clemens-Kirche") == "Büsum"; ("Büsum - Perlebucht") == "Büsum, Perlebucht".
    - Regression echte Multi-Church: extract_boyens_location("Heide St.-Jürgen-Kirche") == "Heide, St.-Jürgen" (Heide/Brunsbüttel bleiben multi_church).
    - Pahlen/Wrohm bereits Single-Church (waren nie in Liste): extract_boyens_location("Pahlen Kirche") == "Pahlen"? — Achtung: Single-Church-Orte ohne expliziten Kirchennamen liefern via _strip_church_suffix "Pahlen", der Wrapper ergänzt "Pahlen, Kirche". Erwartung prüfen und als Test fixieren (Soll: "Pahlen, Kirche").
  </behavior>
  <action>
    (a) churchdesk_api.py — _DEFAULT_MULTI_CHURCH_CITIES (Zeile 226): 'burg' und 'marne' ENTFERNEN. Neue Liste: ['heide', 'brunsbüttel', 'büsum']. 'wrohm'/'pahlen' waren nie drin (bereits Single-Church) — nicht anfassen. Büsum BLEIBT (Perlebucht-/St.Clemens-Sonderlogik in _resolve_location hängt daran).
       Effekt: Burg/Marne durchlaufen jetzt den Einzelkirchen-Zweig (Schritt 4/5 in _resolve_location) → "Ort, Kirche" via Wrapper.
    (b) Neue Migration web/migrations/versions/c3d4e5f6a7b8_fix_single_church_burg_marne.py:
       - revision = 'c3d4e5f6a7b8', down_revision = 'b2c3d4e5f6a7', branch_labels = None, depends_on = None.
       - upgrade(): IDEMPOTENTE Daten-Korrektur via op.execute — `DELETE FROM location_rules WHERE kind='multi_church' AND key IN ('burg','marne')`. (DELETE ist idempotent: läuft auf bereits-gelöschten Rows wirkungslos.) KEIN Schema-Change.
       - downgrade(): `INSERT INTO location_rules (kind, key, value, is_active) VALUES ('multi_church','burg','',1),('multi_church','marne','',1)` (Wiederherstellung des Seed-Zustands; bei UniqueConstraint-Konflikt tolerieren — alternativ INSERT OR IGNORE für SQLite).
       - Hinweis: DB ist SQLite (./data Volume). op.execute mit reinem SQL ist DB-portabel genug; für downgrade INSERT bei SQLite `INSERT OR IGNORE` nutzen, um Idempotenz zu wahren.
       - Migration läuft automatisch beim Container-Start (entrypoint.sh: flask db upgrade) — kein manueller DB-Eingriff auf dem Server nötig.
    (c) Problem 6 (Ort-fehlt rot + nicht preselect): Bereits implementiert (analyze_event_completeness setzt is_complete=False bei fehlender location; Template setzt checkbox nur `checked` wenn is_complete, und border-red-400 wenn `not event.location`). Nur VERIFIZIEREN, keine Änderung nötig falls Logik greift. Falls extract_boyens_location für Urlauberseelsorge-Anzeige eine nicht-leere location liefert obwohl real keine Kirche da ist: NICHT nachschärfen (Simon: drinlassen, nur markieren). Im SUMMARY bestätigen dass die bestehende Logik die Juli-Beispiele (19.7. Büsum ohne Kirche, 22.7. ohne Ort) korrekt rot + nicht-angehakt darstellt.
    (d) Tests: In test_formatting.py die extract_boyens_location-Parametrisierung um Burg/Marne/Pahlen-Fälle (Behavior oben) ergänzen, plus Regressions-Asserts Büsum/Heide.
    String-Formatierung .format(), keine f-strings.
  </action>
  <verify>
    <automated>cd web && python -m pytest tests/test_formatting.py -q -k "location or boyens"</automated>
  </verify>
  <done>Burg/Marne liefern "Ort, Kirche"; Büsum/Heide-Regression grün; idempotente Migration c3d4e5f6a7b8 mit down_revision b2c3d4e5f6a7 existiert; Ort-fehlt-Markierung verifiziert.</done>
</task>

</tasks>

<verification>
Voller Testlauf nach allen drei Tasks:
```
cd web && python -m pytest tests/ -q
```
Alle bestehenden Tests (test_formatting, test_boyens_goldstandard, test_admin, test_auth, test_app_factory) MÜSSEN grün bleiben (keine Regression). Neue Tests (test_routes_defensive + erweiterte test_formatting) grün.

Migration-Sanity (lokal, ohne Server):
```
cd web && python -c "import importlib.util,glob; [importlib.util.spec_from_file_location('m',p) for p in glob.glob('migrations/versions/c3d4e5f6a7b8_*.py')]"
```
Stellt sicher, dass die neue Migration syntaktisch lädt und down_revision = 'b2c3d4e5f6a7' gesetzt ist.
</verification>

<success_criteria>
- Kein "None"-String mehr im Boyens-Output (Excel- und ChurchDesk-Pfad).
- Export/Bearbeiten wirft bei Events mit fehlenden/None-Feldern keinen Serverfehler.
- Vikar:in → "Vik. Nachname", Pröpst:in → "Pr. Nachname".
- Burg/Marne → "Ort, Kirche"; Büsum/Heide-Sonderfälle unverändert.
- Idempotente DB-Migration korrigiert produktive multi_church-Seeds beim nächsten Container-Start.
- Events ohne Ort bleiben in der Liste, sind rot markiert und nicht vorausgewählt.
- `cd web && python -m pytest tests/ -q` ist vollständig grün.
</success_criteria>

<post_execution>
NACH grünen Tests (führt der Orchestrator aus, NICHT als Executor-Commit):

Deployment auf Produktiv-Server (CLAUDE.md):
```
git add . && git commit -m "fix: Boyens-Output-Fixes Juli (None-Härtung, Vik./Pr., Single-Church Burg/Marne, Serverfehler)" && git push && ssh root@185.248.143.234 "cd /opt/gottesdienst-formatter && git pull origin main && cd web && docker compose build && docker compose down && docker compose up -d"
```
Die Daten-Migration c3d4e5f6a7b8 läuft beim Container-Start automatisch (entrypoint.sh → flask db upgrade) und entfernt Burg/Marne aus den multi_church-Regeln der Produktiv-DB.

Daten-Pflege-Hinweis für Simon (manuell über Admin-UI, falls nötig):
- Vikar:innen, die in der ChurchDesk-API OHNE Titel ("Vorname Nachname") ankommen (z.B. "Laura Karlsen"), müssen als Pastor-Eintrag mit title='Vik.' angelegt werden, damit der DB-Lookup greift. Mit Titel im String ("Vikarin Laura Karlsen") funktioniert es ohne DB-Eintrag.
</post_execution>

<output>
Create `.planning/quick/260617-u0x-boyens-output-fixes-juli-feedback/260617-u0x-SUMMARY.md` when done
</output>
