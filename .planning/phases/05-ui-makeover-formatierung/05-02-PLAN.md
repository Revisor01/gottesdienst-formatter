---
phase: 05-ui-makeover-formatierung
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/main/routes.py
  - web/requirements.txt
  - web/templates/index.html
  - web/templates/result.html
autonomous: true
requirements: [UI-03, UI-04]

must_haves:
  truths:
    - "Excel-Upload-Route ist nicht mehr erreichbar (/upload liefert 404)"
    - "pandas und openpyxl sind nicht mehr in requirements.txt"
    - "index.html zeigt nur ChurchDesk-API-Bereich, keinen Excel-Upload"
    - "result.html zeigt Vorschau in Monospace-Font mit prominentem Copy+Download"
    - "Warnungen erscheinen als gelbe Alert-Boxen oberhalb des Textes"
  artifacts:
    - path: "web/main/routes.py"
      provides: "Bereinigter Route-Code ohne Excel-Funktionen"
    - path: "web/templates/index.html"
      provides: "ChurchDesk-only Hauptseite mit Tailwind"
    - path: "web/templates/result.html"
      provides: "Vorschau-Seite mit Monospace, Copy, Download"
  key_links:
    - from: "web/templates/index.html"
      to: "web/main/routes.py"
      via: "form action fetch_churchdesk_events"
      pattern: "url_for.*fetch_churchdesk_events"
    - from: "web/templates/result.html"
      to: "web/main/routes.py"
      via: "form action download_file"
      pattern: "url_for.*download_file"
---

<objective>
Excel-Upload komplett entfernen und Haupt-Templates (index, result) auf Tailwind redesignen.

Purpose: Die einzige Datenquelle ist ChurchDesk-API; die Vorschau zeigt formatierten Text mit Warnungen.
Output: Bereinigter Code ohne Excel-Abhaengigkeiten, moderne Tailwind-Templates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-makeover-formatierung/05-01-SUMMARY.md

<interfaces>
<!-- Aus base.html (Plan 01): Sidebar-Layout mit Tailwind -->
<!-- Templates muessen {% extends "base.html" %} und {% block content %} verwenden -->
<!-- CSS-Klassen aus input.css: btn-primary, btn-secondary, btn-outline, card, input-field, alert-warning, alert-success, alert-danger, alert-info -->
<!-- Sidebar ist 14rem breit (w-56), Content hat md:ml-56 p-4 md:p-8 -->

Aus web/main/routes.py (bestehend):
- index(): render_template('index.html', organizations=ORGANIZATIONS)
- fetch_churchdesk_events(): POST, rendert churchdesk_events.html
- export_selected_events(): POST, rendert result.html mit formatted_text, count, source
- download_file(): POST, sendet .txt Datei
- upload_file(): POST — ZU ENTFERNEN
- process_excel_file(): — ZU ENTFERNEN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Excel-Code entfernen und routes.py bereinigen</name>
  <files>web/main/routes.py, web/requirements.txt</files>
  <read_first>web/main/routes.py, web/requirements.txt</read_first>
  <action>
1. **In web/main/routes.py entfernen:**
   - `import pandas as pd` (Zeile 8 — wird von formatting.py noch gebraucht, dort behalten!)
   - ACHTUNG: Pruefen ob `pd` noch in routes.py direkt verwendet wird. `process_excel_file` nutzt `pd.read_excel` und `pd.isna` — das faellt weg. Aber `convert_churchdesk_events_to_boyens` nutzt KEIN pd. Also: `import pandas as pd` aus routes.py entfernen.
   - Komplette Funktion `process_excel_file()` entfernen (Zeilen 94-139)
   - Komplette Route `upload_file()` entfernen (Zeilen 212-245)
   - `import tempfile` entfernen (nur von upload_file benutzt)
   - Pruefen ob `import io` noch gebraucht wird — ja, von `download_file()`. Behalten.
   - Pruefen ob `import os` noch gebraucht wird — pruefen Nutzung. `os.unlink` war nur in upload. Aber `os` wird ggf. nicht mehr direkt gebraucht. Sicherheitshalber behalten falls andere Imports es nutzen.

2. **In web/requirements.txt entfernen:**
   - `pandas==2.3.1` entfernen
   - `openpyxl==3.1.5` entfernen
   ACHTUNG: `web/formatting.py` importiert `pandas as pd` fuer `pd.isna()` und Datums-Handling!
   - In `web/formatting.py`: `import pandas as pd` durch Standard-Python ersetzen:
     - `pd.isna(date_obj)` → `date_obj is None`
     - `pd.isna(titel)` → `titel is None or titel == ''` oder `not titel`
   - Dann kann pandas komplett raus.

3. **In web/formatting.py anpassen (pandas-Abhaengigkeit entfernen):**
   - `import pandas as pd` entfernen
   - `format_date()`: `if pd.isna(date_obj):` → `if date_obj is None:`
   - `format_time()`: `if pd.isna(date_obj):` → `if date_obj is None:`
   - `format_service_type()`: `if pd.isna(titel):` → `if not titel:`

4. **Ergebnis-Template result.html auf "ChurchDesk Events verarbeitet" anpassen:**
   - Entferne den "source == 'ChurchDesk'" Conditional — es gibt nur noch ChurchDesk.
   - Aendere den Erfolgstext auf "Die Gottesdienste wurden erfolgreich in das Boyens-Format konvertiert."
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && python -c "from web.main.routes import bp; print('routes import OK')" && python -c "from web.formatting import format_date, format_time, format_service_type; print('formatting import OK')" && grep -c "pandas" web/requirements.txt; test $? -eq 1 && echo "pandas removed from requirements" && python -m pytest web/tests/ -v -x</automated>
  </verify>
  <done>
    - upload_file Route und process_excel_file Funktion entfernt
    - pandas und openpyxl aus requirements.txt entfernt
    - formatting.py hat keine pandas-Abhaengigkeit mehr
    - Alle bestehenden Tests bestehen weiterhin
  </done>
</task>

<task type="auto">
  <name>Task 2: index.html und result.html Tailwind-Redesign</name>
  <files>web/templates/index.html, web/templates/result.html</files>
  <read_first>web/templates/index.html, web/templates/result.html, web/templates/base.html</read_first>
  <action>
1. **index.html komplett neu schreiben** (nur ChurchDesk, kein Excel):
   - Kein Bootstrap, nur Tailwind-Klassen (card, btn-primary, input-field aus input.css)
   - Layout: Volle Breite innerhalb des Content-Bereichs (Sidebar ist schon in base.html)
   - Struktur:
     ```
     <h1>Gottesdienste abrufen</h1>
     <div class="card p-6">
       <form ...>
         <!-- Jahr/Monat Selects nebeneinander (grid grid-cols-2 gap-4) -->
         <!-- Org-Auswahl als Card mit Checkboxen -->
         <!-- Submit-Button gruen, volle Breite -->
       </form>
     </div>
     ```
   - Jahr-Select: Dynamisch aktuelles Jahr + naechstes Jahr, aktuelles vorausgewaehlt
   - Monat-Select: Aktueller Monat vorausgewaehlt (bestehende JS-Logik beibehalten)
   - Org-Checkboxen in einer Card:
     ```html
     <div class="card p-4 mt-4">
       <div class="flex items-center justify-between mb-3">
         <h3 class="font-semibold text-gray-700">Kirchspiele</h3>
         <label class="flex items-center gap-2 text-sm">
           <input type="checkbox" id="select-all-orgs" checked class="rounded text-primary-600">
           Alle
         </label>
       </div>
       <div class="grid grid-cols-1 md:grid-cols-2 gap-2">
         {% for org_id, org in organizations.items() %}
         <label class="flex items-start gap-2 p-2 rounded hover:bg-gray-50 cursor-pointer">
           <input type="checkbox" name="selected_organizations" value="{{ org_id }}" class="mt-1 rounded text-primary-600" checked>
           <div>
             <span class="font-medium">{{ org.name }}</span>
             {% if org.description %}<br><span class="text-sm text-gray-500">{{ org.description }}</span>{% endif %}
           </div>
         </label>
         {% endfor %}
       </div>
     </div>
     ```
   - "Keine Organisationen" Warnung als alert-warning
   - Submit-Button: `class="btn-primary w-full text-lg mt-4"`
   - Excel-Upload-Bereich komplett weg (kein "oder"-Divider, keine Excel-Card, keine Anforderungen-Card)
   - Select-All JS-Logik beibehalten (bestehend, funktioniert)
   - Monat-Default-Logik beibehalten

2. **result.html komplett neu schreiben** mit Tailwind:
   - Layout:
     ```
     <div class="flex items-center justify-between mb-6">
       <h1>Formatierte Gottesdiensttermine</h1>
       <span class="badge">{{ count }} Termine</span>
     </div>
     <!-- Warnungen (gelbe Alert-Boxen) oberhalb des Textes -->
     {% if warnings %}
     {% for warning in warnings %}
     <div class="alert-warning flex items-start gap-2 mb-3">
       <svg ...warning icon.../> {{ warning }}
     </div>
     {% endfor %}
     {% endif %}
     <!-- Action-Buttons: Copy + Download prominent nebeneinander -->
     <div class="flex gap-3 mb-4">
       <button onclick="copyToClipboard()" class="btn-primary flex items-center gap-2">
         <!-- Clipboard SVG --> In Zwischenablage kopieren
       </button>
       <form method="POST" action="{{ url_for('main.download_file') }}">
         <input type="hidden" name="csrf_token" value="{{ csrf_token() }}"/>
         <input type="hidden" name="formatted_text" value="{{ formatted_text }}">
         <button type="submit" class="btn-outline flex items-center gap-2">
           <!-- Download SVG --> Als .txt herunterladen
         </button>
       </form>
     </div>
     <!-- Vorschau-Box: Monospace-Font -->
     <div class="card p-6">
       <pre class="font-mono text-sm whitespace-pre-wrap text-gray-800 leading-relaxed" id="resultText">{{ formatted_text }}</pre>
     </div>
     <!-- Zurueck-Button -->
     <div class="mt-4">
       <a href="{{ url_for('main.index') }}" class="btn-secondary">Zurueck zur Uebersicht</a>
     </div>
     ```
   - Copy-to-Clipboard JS beibehalten (bestehend, funktioniert), aber Button-Styling auf Tailwind
   - Kopier-Feedback: Button wird kurz gruen mit Haekchen-Icon
   - "Naechste Schritte"-Card entfernen (wird in Phase 6 durch Auto-Mail ersetzt)
   - Kein `source == 'ChurchDesk'` Conditional mehr — es gibt nur noch ChurchDesk
   - Excel-Referenzen aus dem Erfolgstext entfernen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && python -c "
from web.app import create_app
import os
os.environ['SECRET_KEY'] = 'test'
app = create_app()
client = app.test_client()
# Login nicht noetig fuer Template-Check, pruefe nur ob Templates rendern
with app.test_request_context():
    from flask import render_template
    from config import ORGANIZATIONS
    html = render_template('index.html', organizations=ORGANIZATIONS)
    assert 'tailwind' not in html.lower() or 'output.css' in html.lower() or 'btn-primary' in html
    assert 'Excel' not in html
    assert 'upload' not in html.lower() or 'upload' not in html
    print('index.html: OK - kein Excel, Tailwind-Klassen vorhanden')
    html2 = render_template('result.html', formatted_text='Test', count=1, source='ChurchDesk')
    assert 'font-mono' in html2
    print('result.html: OK - Monospace vorhanden')
print('Template-Check bestanden')
"</automated>
  </verify>
  <done>
    - index.html zeigt nur ChurchDesk-API-Bereich, kein Excel-Upload sichtbar
    - Org-Checkboxen in Card mit Tailwind-Styling
    - result.html zeigt Vorschau in Monospace-Font
    - Copy-to-Clipboard und Download prominent positioniert
    - Alle Bootstrap-Klassen durch Tailwind ersetzt
  </done>
</task>

</tasks>

<verification>
- `/upload` Route existiert nicht mehr (404 bei POST)
- `grep -r "pandas" web/requirements.txt` liefert keinen Treffer
- `grep -r "Excel" web/templates/index.html` liefert keinen Treffer
- `python -m pytest web/tests/ -v` — alle Tests bestehen
- index.html und result.html enthalten keine Bootstrap-Klassen
</verification>

<success_criteria>
- Excel-Code komplett entfernt (Route, Funktion, Dependencies)
- formatting.py hat keine pandas-Abhaengigkeit mehr
- index.html zeigt ausschliesslich ChurchDesk-API-Interface
- result.html zeigt Monospace-Vorschau mit prominenten Copy+Download-Buttons
- Warnungen als gelbe Alert-Boxen implementiert
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-makeover-formatierung/05-02-SUMMARY.md`
</output>
