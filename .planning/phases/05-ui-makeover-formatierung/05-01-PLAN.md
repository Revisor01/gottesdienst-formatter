---
phase: 05-ui-makeover-formatierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tailwind.config.js
  - web/static/css/input.css
  - web/static/css/output.css
  - web/templates/base.html
  - web/formatting.py
  - web/requirements.txt
  - .github/workflows/ci-cd.yml
  - web/Dockerfile
  - package.json
autonomous: true
requirements: [UI-01, UI-02, FMT-10]

must_haves:
  truths:
    - "Tailwind CSS CLI baut output.css aus input.css ohne Fehler"
    - "base.html verwendet Tailwind-Klassen statt Bootstrap, zeigt gruenes Sidebar-Layout"
    - "GitHub Actions baut Tailwind CSS vor dem Docker-Build"
    - "Sonderformat-Titel mit Doppelpunkt werden korrekt geparst (Gd. m. A. Erntedank)"
    - "Anfuehrungszeichen-Titel werden 1:1 uebernommen ohne Typ-Matching"
  artifacts:
    - path: "web/tailwind.config.js"
      provides: "Tailwind-Konfiguration mit gruenem Primaerfarbschema"
    - path: "web/static/css/input.css"
      provides: "Tailwind-Direktiven und Custom-Styles"
    - path: "web/static/css/output.css"
      provides: "Kompiliertes CSS-Artefakt"
    - path: "web/templates/base.html"
      provides: "Sidebar-Layout mit Tailwind-Klassen"
  key_links:
    - from: "web/templates/base.html"
      to: "web/static/css/output.css"
      via: "link-Tag im HTML-Head"
      pattern: "static/css/output.css"
    - from: ".github/workflows/ci-cd.yml"
      to: "web/static/css/output.css"
      via: "Tailwind CLI Build-Step vor Docker-Build"
      pattern: "tailwindcss"
---

<objective>
Tailwind CSS CLI einrichten, base.html auf Sidebar-Dashboard-Layout umbauen, Sonderformat-Parsing verbessern.

Purpose: Fundament fuer alle Template-Redesigns in Plan 02/03 schaffen und FMT-10 unabhaengig davon implementieren.
Output: Funktionierender Tailwind-Build, gruenes Sidebar-Base-Template, korrektes Sonderformat-Parsing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@web/formatting.py
@web/templates/base.html
@.github/workflows/ci-cd.yml
@web/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind CSS CLI Setup und base.html Sidebar-Layout</name>
  <files>web/tailwind.config.js, web/static/css/input.css, web/static/css/output.css, web/templates/base.html, web/requirements.txt, .github/workflows/ci-cd.yml, web/Dockerfile, package.json</files>
  <read_first>web/templates/base.html, .github/workflows/ci-cd.yml, web/Dockerfile, web/requirements.txt</read_first>
  <action>
1. **Tailwind CLI installieren und konfigurieren:**
   - Im Projekt-Root `package.json` erstellen (nur fuer Tailwind-Build, NICHT fuer Docker):
     ```json
     {
       "private": true,
       "scripts": {
         "tw:build": "npx @tailwindcss/cli -i web/static/css/input.css -o web/static/css/output.css --minify",
         "tw:watch": "npx @tailwindcss/cli -i web/static/css/input.css -o web/static/css/output.css --watch"
       },
       "devDependencies": {
         "@tailwindcss/cli": "^4.1.0"
       }
     }
     ```
   - `web/tailwind.config.js` erstellen:
     ```js
     /** @type {import('tailwindcss').Config} */
     module.exports = {
       content: ['./web/templates/**/*.html'],
       theme: {
         extend: {
           colors: {
             primary: {
               50: '#f0fdf4',
               100: '#dcfce7',
               200: '#bbf7d0',
               300: '#86efac',
               400: '#4ade80',
               500: '#22c55e',
               600: '#16a34a',
               700: '#15803d',
               800: '#166534',
               900: '#14532d',
             }
           }
         }
       },
       plugins: [],
     }
     ```
   - `web/static/css/input.css` erstellen:
     ```css
     @tailwind base;
     @tailwind components;
     @tailwind utilities;

     @layer components {
       .btn-primary {
         @apply bg-primary-600 hover:bg-primary-700 text-white font-medium py-2 px-4 rounded-lg transition-colors;
       }
       .btn-secondary {
         @apply bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium py-2 px-4 rounded-lg transition-colors;
       }
       .btn-outline {
         @apply border border-primary-600 text-primary-700 hover:bg-primary-50 font-medium py-2 px-4 rounded-lg transition-colors;
       }
       .card {
         @apply bg-white rounded-lg shadow-sm border border-gray-200;
       }
       .input-field {
         @apply w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 outline-none;
       }
       .alert-warning {
         @apply bg-yellow-50 border border-yellow-200 text-yellow-800 px-4 py-3 rounded-lg;
       }
       .alert-success {
         @apply bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg;
       }
       .alert-danger {
         @apply bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg;
       }
       .alert-info {
         @apply bg-blue-50 border border-blue-200 text-blue-800 px-4 py-3 rounded-lg;
       }
     }
     ```
   - WICHTIG: Tailwind v4 verwendet `@tailwindcss/cli` statt `tailwindcss`. Pruefen ob v4 die config.js noch unterstuetzt — falls nicht, auf v3 (`tailwindcss: ^3.4.0`) zurueckfallen und `npx tailwindcss` verwenden.
   - `npm install` ausfuehren und `npm run tw:build` testen — output.css muss erzeugt werden.

2. **base.html komplett ersetzen** (Bootstrap raus, Tailwind rein):
   - Bootstrap CDN-Links entfernen, stattdessen `<link href="{{ url_for('static', filename='css/output.css') }}" rel="stylesheet">`
   - Sidebar-Dashboard-Layout (per CONTEXT.md Decision):
     - Sidebar links: 64px breit (Icons) auf Desktop, voll ausklappbar auf Hover/Click
     - Sidebar-Items: Logo oben, Navigation (Formatter, Benutzer fuer Admin), Username + Logout unten
     - Content rechts: `ml-16` mit `max-w-7xl mx-auto` padding
     - Responsive 768px: Sidebar wird Hamburger-Menu (hidden by default, toggle via JS)
   - Gruen als Primaerfarbe: Sidebar-Hintergrund `bg-primary-700`, Hover `bg-primary-600`
   - Flash-Messages: Jinja2 `get_flashed_messages(with_categories=true)` beibehalten, aber mit Tailwind-Alert-Klassen
   - Footer entfernen (Sidebar-Layout braucht keinen Footer)
   - Bootstrap JS CDN entfernen — kein jQuery/Bootstrap JS mehr noetig
   - Hamburger-Menu JS inline im Template (wenige Zeilen toggle)

   Exaktes HTML-Grundgeruest fuer base.html:
   ```html
   <!DOCTYPE html>
   <html lang="de">
   <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>{% block title %}Gottesdienst-Formatter{% endblock %}</title>
     <link href="{{ url_for('static', filename='css/output.css') }}" rel="stylesheet">
   </head>
   <body class="bg-gray-50 min-h-screen">
     <!-- Mobile Header -->
     <header class="md:hidden bg-primary-700 text-white px-4 py-3 flex items-center justify-between">
       <span class="font-semibold">Gottesdienst-Formatter</span>
       <button id="menu-toggle" class="p-1">
         <!-- Hamburger SVG icon -->
       </button>
     </header>
     <!-- Sidebar -->
     <aside id="sidebar" class="fixed inset-y-0 left-0 w-56 bg-primary-700 text-white transform -translate-x-full md:translate-x-0 transition-transform z-30">
       <!-- Logo -->
       <!-- Nav links -->
       <!-- User/Logout am unteren Rand -->
     </aside>
     <!-- Overlay fuer Mobile -->
     <div id="sidebar-overlay" class="fixed inset-0 bg-black/50 z-20 hidden md:hidden"></div>
     <!-- Main Content -->
     <main class="md:ml-56 p-4 md:p-8">
       <!-- Flash Messages -->
       {% block content %}{% endblock %}
     </main>
     <script>
       // Hamburger toggle: sidebar + overlay ein/ausblenden
     </script>
   </body>
   </html>
   ```

3. **CI/CD Tailwind Build-Step:**
   - In `.github/workflows/ci-cd.yml` im `build-and-push` Job VOR dem Docker-Build:
     ```yaml
     - uses: actions/setup-node@v4
       with:
         node-version: '20'
     - name: Build Tailwind CSS
       run: |
         npm ci
         npm run tw:build
     ```
   - Im `test` Job ebenfalls Tailwind bauen falls Templates getestet werden.

4. **Dockerfile anpassen:**
   - KEIN Node.js im Container — CSS wird vorab gebaut (per Decision).
   - `web/static/css/output.css` muss vor `docker build` existieren.
   - Der CI/CD-Workflow baut CSS, dann `docker build` kopiert es mit `COPY . .`
   - Kein Aenderung am Dockerfile noetig, da `COPY . .` bereits alles kopiert.
   - Falls `web/static/` Verzeichnis nicht existiert, sicherstellen dass es im Repo ist (ggf. `.gitkeep`).

5. **output.css ins .gitignore:**
   - `web/static/css/output.css` NICHT in .gitignore — es wird im CI gebaut und muss im Docker-Context sein.
   - Alternativ: output.css committen fuer lokale Entwicklung. Entscheidung: output.css committen (einfacher fuer lokale Dev).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && npm install && npm run tw:build && test -f web/static/css/output.css && echo "OK: output.css exists" && wc -c web/static/css/output.css</automated>
  </verify>
  <done>
    - tailwind.config.js konfiguriert mit gruenem Primaerfarbschema
    - input.css mit Tailwind-Direktiven und Component-Layer existiert
    - output.css wird fehlerfrei gebaut
    - base.html zeigt Sidebar-Layout mit gruenem Hintergrund, kein Bootstrap
    - CI/CD baut Tailwind vor Docker-Build
    - Responsive: Hamburger-Menu auf Mobile, Sidebar auf Desktop
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FMT-10 Sonderformat-Parsing in format_service_type</name>
  <files>web/formatting.py, web/tests/test_formatting.py</files>
  <read_first>web/formatting.py, web/tests/test_formatting.py</read_first>
  <behavior>
    - "Gottesdienst mit Abendmahl: Erntedank" -> "Gd. m. A. Erntedank"
    - "Gottesdienst mit Taufe: Familiengottesdienst" -> "Gd. m. T. Familiengottesdienst"
    - "Gottesdienst mit Abendmahl und Taufe: Pfingsten" -> "Abendmahlgd. m. T. Pfingsten"
    - "Gottesdienst: Lichterfest" -> "Gd. Lichterfest" (nur "Gottesdienst" vor Doppelpunkt -> "Gd." + Untertitel)
    - "Gottesdienst:" (nur Doppelpunkt, kein Untertitel) -> "Gd."
    - Titel mit Anfuehrungszeichen (typografisch oder gerade) -> komplett 1:1 uebernehmen, KEIN Typ-Matching
    - Bestehende Tests duerfen nicht brechen (Konfirmation, Abendmahl, etc.)
  </behavior>
  <action>
In `web/formatting.py` die Funktion `format_service_type()` erweitern:

1. **Anfuehrungszeichen-Check bleibt wie er ist** (bereits implementiert, Zeile 56).

2. **NEU: Doppelpunkt-Split VOR dem generischen Matching einfuegen** (nach Anfuehrungszeichen-Check, vor allen elif-Zweigen):
   ```python
   # FMT-10: Doppelpunkt-Split fuer Typ + Untertitel
   if ':' in titel:
       parts = titel.split(':', 1)
       typ_teil = parts[0].strip()
       untertitel = parts[1].strip() if len(parts) > 1 else ''

       # Typ-Teil durch normales Matching schicken
       typ_formatiert = _match_service_type(typ_teil)

       if untertitel:
           return '{} {}'.format(typ_formatiert, untertitel)
       else:
           return typ_formatiert
   ```

3. **Refactor: Bestehende elif-Kette in _match_service_type(titel) extrahieren:**
   - Neue private Funktion `_match_service_type(titel: str) -> str`
   - Nimmt den bisherigen elif-Block (Tauffest, Konfirmation, Abendmahl, etc.)
   - Gibt den formatierten Typ zurueck
   - `format_service_type()` ruft `_match_service_type()` auf fuer Titel ohne Doppelpunkt
   - Fuer Titel MIT Doppelpunkt: nur den Teil vor dem Doppelpunkt durch `_match_service_type()` schicken

4. **Wichtig: Reihenfolge der Checks:**
   - pd.isna check (ganz oben)
   - Anfuehrungszeichen-Check (bleibt)
   - Doppelpunkt-Split (NEU)
   - Fallback: _match_service_type(titel) fuer alles ohne Doppelpunkt

5. **Tests in web/tests/test_formatting.py ergaenzen:**
   - Neue Test-Klasse oder Testfunktionen fuer Sonderformat:
     ```python
     def test_sonderformat_abendmahl_untertitel():
         assert format_service_type("Gottesdienst mit Abendmahl: Erntedank") == "Gd. m. A. Erntedank"

     def test_sonderformat_taufe_untertitel():
         assert format_service_type("Gottesdienst mit Taufe: Familiengottesdienst") == "Gd. m. T. Familiengottesdienst"

     def test_sonderformat_nur_gottesdienst_doppelpunkt():
         assert format_service_type("Gottesdienst: Lichterfest") == "Gd. Lichterfest"

     def test_sonderformat_doppelpunkt_ohne_untertitel():
         assert format_service_type("Gottesdienst:") == "Gd."

     def test_anfuehrungszeichen_unveraendert():
         # Bestehender Test, sicherstellen dass er weiterhin passt
         titel = '\u201eUnterwegs\u201c Brotzeit: Die Wohnzimmerkirche in Heide (Sued)'
         assert format_service_type(titel) == titel
     ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && python -m pytest web/tests/test_formatting.py -v -x</automated>
  </verify>
  <done>
    - format_service_type("Gottesdienst mit Abendmahl: Erntedank") == "Gd. m. A. Erntedank"
    - Anfuehrungszeichen-Titel werden weiterhin 1:1 uebernommen
    - Alle bestehenden Tests in test_formatting.py bestehen weiterhin
    - _match_service_type() als extrahierte Hilfsfunktion vorhanden
  </done>
</task>

</tasks>

<verification>
- `npm run tw:build` erzeugt output.css ohne Fehler
- base.html enthaelt keine Bootstrap-Referenzen mehr
- base.html enthaelt Sidebar mit `bg-primary-700`
- Alle bestehenden Tests bestehen: `python -m pytest web/tests/ -v`
- format_service_type Sonderformat-Tests bestehen
</verification>

<success_criteria>
- Tailwind CSS Build-Pipeline funktioniert lokal und in CI
- base.html ist vollstaendig auf Tailwind umgestellt mit gruenem Sidebar-Layout
- FMT-10 Doppelpunkt-Split korrekt implementiert mit Tests
- Kein Bootstrap mehr im Projekt
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-makeover-formatierung/05-01-SUMMARY.md`
</output>
