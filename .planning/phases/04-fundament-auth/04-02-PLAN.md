---
phase: 04-fundament-auth
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/auth/__init__.py
  - web/auth/routes.py
  - web/auth/forms.py
  - web/templates/auth/login.html
  - web/templates/base.html
  - web/main/routes.py
  - web/app.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-05, FOUND-05]

must_haves:
  truths:
    - "User kann sich mit Benutzername und Passwort einloggen"
    - "Nach Logout oder Session-Ablauf wird auf /login umgeleitet"
    - "Alle bestehenden Formatter-Routes sind ohne Login nicht erreichbar"
    - "/health antwortet mit 200 ohne Login"
    - "CSRF-Token ist in allen Formularen vorhanden"
  artifacts:
    - path: "web/auth/routes.py"
      provides: "Login/Logout-Routes"
      contains: "def login"
    - path: "web/auth/forms.py"
      provides: "LoginForm mit CSRF"
      contains: "class LoginForm"
    - path: "web/templates/auth/login.html"
      provides: "Login-Seite"
      contains: "form.hidden_tag"
  key_links:
    - from: "web/main/routes.py"
      to: "flask_login"
      via: "@login_required Decorator"
      pattern: "@login_required"
    - from: "web/auth/routes.py"
      to: "web/models.py"
      via: "User.query + check_password"
      pattern: "User\\.query\\.filter_by"
    - from: "web/templates/auth/login.html"
      to: "web/auth/forms.py"
      via: "form.hidden_tag() fuer CSRF"
---

<objective>
Auth-System einbauen: Login/Logout, CSRF-Schutz, @login_required auf allen Routes, /health Whitelist.

Purpose: Ohne Auth sind alle ChurchDesk-API-Tokens ueber die Web-Oberflaeche frei zugaenglich (Pitfall 3).
Output: Funktionierende Login-Seite, geschuetzte Routes, /health Endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fundament-auth/04-CONTEXT.md
@.planning/phases/04-fundament-auth/04-01-SUMMARY.md

@web/app.py
@web/extensions.py
@web/models.py
@web/main/routes.py
@web/templates/base.html

<interfaces>
From web/extensions.py (created in Plan 01):
```python
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate
from flask_login import LoginManager
from flask_wtf.csrf import CSRFProtect

db = SQLAlchemy()
migrate = Migrate()
login_manager = LoginManager()
csrf = CSRFProtect()

login_manager.login_view = 'auth.login'
login_manager.login_message = 'Bitte einloggen.'
```

From web/models.py (created in Plan 01):
```python
class User(UserMixin, db.Model):
    __tablename__ = 'users'
    id: int (PK)
    username: str (unique)
    password_hash: str
    is_admin: bool
    is_active_user: bool
    created_at: datetime

    def set_password(self, password): ...
    def check_password(self, password) -> bool: ...
    @property
    def is_active(self) -> bool: ...  # delegates to is_active_user
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Blueprint mit Login/Logout und CSRF-Forms</name>
  <files>web/auth/__init__.py, web/auth/routes.py, web/auth/forms.py, web/templates/auth/login.html</files>
  <read_first>web/app.py, web/extensions.py, web/models.py, web/templates/base.html</read_first>
  <action>
1. Erstelle `web/auth/__init__.py`:

```python
from flask import Blueprint

bp = Blueprint('auth', __name__, url_prefix='')

from auth import routes  # noqa: E402, F401
```

Kein url_prefix — /login soll direkt unter / liegen, nicht unter /auth/login.

2. Erstelle `web/auth/forms.py` mit Flask-WTF (liefert automatisch CSRF via form.hidden_tag()):

```python
from flask_wtf import FlaskForm
from wtforms import StringField, PasswordField, BooleanField, SubmitField
from wtforms.validators import DataRequired, Length


class LoginForm(FlaskForm):
    username = StringField('Benutzername', validators=[DataRequired()])
    password = PasswordField('Passwort', validators=[DataRequired()])
    remember_me = BooleanField('Eingeloggt bleiben')
    submit = SubmitField('Anmelden')
```

3. Erstelle `web/auth/routes.py`:

```python
from flask import render_template, redirect, url_for, flash, request
from flask_login import login_user, logout_user, login_required, current_user
from auth import bp
from models import User
from auth.forms import LoginForm


@bp.route('/login', methods=['GET', 'POST'])
def login():
    if current_user.is_authenticated:
        return redirect(url_for('main.index'))

    form = LoginForm()
    if form.validate_on_submit():
        user = User.query.filter_by(username=form.username.data).first()
        if user is None or not user.check_password(form.password.data):
            flash('Falscher Benutzername oder Passwort', 'danger')
            return redirect(url_for('auth.login'))

        login_user(user, remember=form.remember_me.data)
        next_page = request.args.get('next')
        # Sicherheit: nur relative URLs akzeptieren
        if next_page and not next_page.startswith('/'):
            next_page = None
        return redirect(next_page or url_for('main.index'))

    return render_template('auth/login.html', form=form)


@bp.route('/logout')
@login_required
def logout():
    logout_user()
    flash('Erfolgreich abgemeldet.', 'info')
    return redirect(url_for('auth.login'))
```

4. Erstelle `web/templates/auth/login.html` — einfaches zentriertes Login-Formular (per CONTEXT.md: "minimal, funktional, gruenes Logo"):

```html
{% extends "base.html" %}

{% block title %}Login - Gottesdienst-Formatter{% endblock %}

{% block content %}
<div class="row justify-content-center mt-5">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header text-center">
                <h4 class="mb-0">Anmelden</h4>
            </div>
            <div class="card-body">
                <form method="POST" action="{{ url_for('auth.login') }}">
                    {{ form.hidden_tag() }}
                    <div class="mb-3">
                        {{ form.username.label(class="form-label") }}
                        {{ form.username(class="form-control", autofocus=true) }}
                    </div>
                    <div class="mb-3">
                        {{ form.password.label(class="form-label") }}
                        {{ form.password(class="form-control") }}
                    </div>
                    <div class="mb-3 form-check">
                        {{ form.remember_me(class="form-check-input") }}
                        {{ form.remember_me.label(class="form-check-label") }}
                    </div>
                    <div class="d-grid">
                        {{ form.submit(class="btn btn-primary") }}
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine/web && test -f auth/__init__.py && test -f auth/routes.py && test -f auth/forms.py && test -f templates/auth/login.html && python -c "from auth.forms import LoginForm; print('forms OK')" && echo "Auth blueprint files OK"</automated>
  </verify>
  <done>Auth-Blueprint existiert mit Login/Logout-Routes. LoginForm hat CSRF (hidden_tag). Login-Template ist zentriert und minimal.</done>
</task>

<task type="auto">
  <name>Task 2: @login_required auf alle Routes + /health + Blueprint-Registrierung + base.html</name>
  <files>web/main/routes.py, web/app.py, web/templates/base.html, web/templates/index.html, web/templates/result.html, web/templates/churchdesk_events.html</files>
  <read_first>web/main/routes.py, web/app.py, web/templates/base.html, web/templates/index.html</read_first>
  <action>
1. Ergaenze `web/main/routes.py` — ALLE bestehenden Routes mit `@login_required` schuetzen:

```python
from flask_login import login_required
```

Decorator auf JEDE Route (ausser /health):
- `@bp.route('/')` → `@login_required` darunter
- `@bp.route('/upload', ...)` → `@login_required` darunter
- `@bp.route('/download', ...)` → `@login_required` darunter
- `@bp.route('/fetch_churchdesk_events', ...)` → `@login_required` darunter
- `@bp.route('/export_selected_events', ...)` → `@login_required` darunter

Reihenfolge: `@bp.route(...)` zuerst, dann `@login_required`, dann `def ...`.

2. Fuege /health Route in main/routes.py hinzu — OHNE @login_required (FOUND-05, Pitfall: Docker Healthcheck + Uptime Kuma brauchen ungeschuetzten Zugang):

```python
@bp.route('/health')
def health():
    return {'status': 'ok'}, 200
```

3. Registriere auth-Blueprint in `web/app.py` — nach main-Blueprint:

```python
from auth import bp as auth_bp
app.register_blueprint(auth_bp)
```

4. Aktualisiere `web/templates/base.html` — Navbar um Login-Status und Logout-Link erweitern:

In der Navbar nach dem Brand-Link, vor dem schliessenden `</div>` des Containers:

```html
{% if current_user.is_authenticated %}
<div class="navbar-nav ms-auto">
    <span class="navbar-text me-3">{{ current_user.username }}</span>
    <a class="nav-link" href="{{ url_for('auth.logout') }}">Abmelden</a>
    {% if current_user.is_admin %}
    <a class="nav-link" href="{{ url_for('admin.users') }}">Benutzer</a>
    {% endif %}
</div>
{% endif %}
```

Hinweis: Der admin.users-Link wird erst in Plan 04 funktionieren — das ist OK, er wird nur angezeigt wenn der Admin-Blueprint registriert ist. Alternativ den Link mit `{% if 'admin.users' in url_map %}` absichern oder einfacher: den Admin-Link erst in Plan 04 hinzufuegen. Entscheide pragmatisch: fuege den Admin-Link NICHT hinzu, nur den Logout-Link und Username. Plan 04 ergaenzt den Admin-Link.

5. CSRF-Token in bestehende Templates einbauen — alle `<form>`-Tags in index.html, result.html, churchdesk_events.html brauchen `{{ csrf_token() }}` als Hidden-Field:

In jedem `<form>` Tag nach dem oeffnenden Tag einfuegen:
```html
<input type="hidden" name="csrf_token" value="{{ csrf_token() }}"/>
```

ACHTUNG: Das betrifft NUR die bestehenden Forms die NICHT Flask-WTF nutzen (die nutzen einfache HTML-Forms). Der LoginForm hat hidden_tag() — das ist schon abgedeckt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && source venv/bin/activate && SECRET_KEY=test python -c "
from web.app import create_app
app = create_app()
with app.test_client() as c:
    # Index ohne Login → Redirect
    r = c.get('/')
    assert r.status_code == 302, f'Index should redirect, got {r.status_code}'
    assert '/login' in r.headers['Location'], 'Should redirect to /login'
    # Health ohne Login → 200
    r = c.get('/health')
    assert r.status_code == 200, f'Health should be 200, got {r.status_code}'
    # Login-Seite → 200
    r = c.get('/login')
    assert r.status_code == 200, f'Login should be 200, got {r.status_code}'
    print('All auth checks passed')
"</automated>
  </verify>
  <done>Alle 5 bestehenden Routes leiten ohne Login auf /login um. /health gibt 200 ohne Login. /login zeigt Login-Formular. CSRF-Tokens in allen Forms. Logout-Link in Navbar sichtbar wenn eingeloggt.</done>
</task>

<task type="auto">
  <name>Task 3: Auth-Tests</name>
  <files>tests/test_auth.py</files>
  <read_first>web/auth/routes.py, web/models.py, tests/test_app_factory.py</read_first>
  <action>
Erstelle `tests/test_auth.py`:

```python
import pytest
import os

os.environ['SECRET_KEY'] = 'test-secret-key-for-unit-tests'


def create_test_app():
    from web.app import create_app
    app = create_app()
    app.config['TESTING'] = True
    app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite://'
    app.config['WTF_CSRF_ENABLED'] = False
    from web.extensions import db
    with app.app_context():
        db.create_all()
    return app


def create_test_user(app, username='testuser', password='testpass123', is_admin=False):
    from web.extensions import db
    from web.models import User
    with app.app_context():
        user = User(username=username, is_admin=is_admin)
        user.set_password(password)
        db.session.add(user)
        db.session.commit()
    return username, password


class TestProtectedRoutes:
    """Alle bestehenden Routes muessen ohne Login auf /login umleiten."""

    @pytest.fixture(autouse=True)
    def setup(self):
        self.app = create_test_app()
        self.client = self.app.test_client()

    @pytest.mark.parametrize('path', [
        '/',
        '/upload',
        '/download',
        '/fetch_churchdesk_events',
        '/export_selected_events',
    ])
    def test_redirect_to_login(self, path):
        resp = self.client.get(path)
        assert resp.status_code in (302, 308), f'{path} should redirect, got {resp.status_code}'
        assert '/login' in resp.headers.get('Location', '')

    def test_health_no_login_required(self):
        resp = self.client.get('/health')
        assert resp.status_code == 200


class TestLogin:
    @pytest.fixture(autouse=True)
    def setup(self):
        self.app = create_test_app()
        self.client = self.app.test_client()
        create_test_user(self.app)

    def test_login_page_loads(self):
        resp = self.client.get('/login')
        assert resp.status_code == 200
        assert b'Anmelden' in resp.data

    def test_login_success(self):
        resp = self.client.post('/login', data={
            'username': 'testuser',
            'password': 'testpass123',
        }, follow_redirects=True)
        assert resp.status_code == 200
        # Nach Login sollte Index-Seite kommen (nicht Login-Formular)
        assert b'Anmelden' not in resp.data or b'Abmelden' in resp.data

    def test_login_wrong_password(self):
        resp = self.client.post('/login', data={
            'username': 'testuser',
            'password': 'falsch',
        }, follow_redirects=True)
        assert b'Falscher Benutzername oder Passwort' in resp.data

    def test_login_unknown_user(self):
        resp = self.client.post('/login', data={
            'username': 'gibtsnet',
            'password': 'egal',
        }, follow_redirects=True)
        assert b'Falscher Benutzername oder Passwort' in resp.data

    def test_logout(self):
        # Login
        self.client.post('/login', data={
            'username': 'testuser',
            'password': 'testpass123',
        })
        # Logout
        resp = self.client.get('/logout', follow_redirects=True)
        assert b'Erfolgreich abgemeldet' in resp.data
        # Nach Logout: Index leitet auf Login um
        resp = self.client.get('/')
        assert resp.status_code == 302

    def test_deactivated_user_cannot_login(self):
        from web.extensions import db
        from web.models import User
        with self.app.app_context():
            user = User.query.filter_by(username='testuser').first()
            user.is_active_user = False
            db.session.commit()
        resp = self.client.post('/login', data={
            'username': 'testuser',
            'password': 'testpass123',
        }, follow_redirects=True)
        # Flask-Login verweigert login_user fuer inaktive User
        assert b'Anmelden' in resp.data
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && source venv/bin/activate && python -m pytest tests/test_auth.py -x -v</automated>
  </verify>
  <done>Alle Auth-Tests bestehen: Login-Redirect, Login-Success, Wrong-Password, Logout, Health-Whitelist, deaktivierter User.</done>
</task>

</tasks>

<verification>
1. `curl -s -o /dev/null -w '%{http_code}' http://localhost:5000/` → 302 (Redirect zu /login)
2. `curl -s -o /dev/null -w '%{http_code}' http://localhost:5000/health` → 200
3. `curl -s -o /dev/null -w '%{http_code}' http://localhost:5000/login` → 200
4. `python -m pytest tests/test_auth.py tests/test_app_factory.py -x -v` → alle gruen
</verification>

<success_criteria>
- Login/Logout funktioniert mit Flask-Login (AUTH-01)
- User-Modell hat Passwort-Hashing via Werkzeug (AUTH-02)
- Alle bestehenden Routes hinter @login_required (AUTH-03)
- CSRF-Schutz auf allen Formularen (AUTH-05)
- /health ohne Login erreichbar (FOUND-05)
</success_criteria>

<output>
After completion, create `.planning/phases/04-fundament-auth/04-02-SUMMARY.md`
</output>
