---
phase: 04-fundament-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app.py
  - web/extensions.py
  - web/models.py
  - web/main/__init__.py
  - web/main/routes.py
  - web/requirements.txt
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-04]

must_haves:
  truths:
    - "create_app() existiert und gibt eine konfigurierte Flask-App zurueck"
    - "SQLAlchemy und Flask-Migrate sind initialisiert und migrations/ existiert im Repo"
    - "SECRET_KEY ohne Fallback — RuntimeError wenn nicht gesetzt"
    - "Alle bestehenden Routes funktionieren unveraendert nach dem Refactor"
  artifacts:
    - path: "web/extensions.py"
      provides: "db, migrate, login_manager, csrf Instanzen"
      exports: ["db", "migrate", "login_manager", "csrf"]
    - path: "web/models.py"
      provides: "User-Modell mit Flask-Login UserMixin"
      contains: "class User"
    - path: "web/app.py"
      provides: "create_app() Factory"
      contains: "def create_app"
    - path: "web/main/routes.py"
      provides: "Bestehende Routes als Blueprint"
      contains: "bp = Blueprint"
    - path: "web/migrations/"
      provides: "Alembic Migrations-Ordner"
  key_links:
    - from: "web/app.py"
      to: "web/extensions.py"
      via: "import und init_app()"
      pattern: "db\\.init_app|migrate\\.init_app"
    - from: "web/main/routes.py"
      to: "web/extensions.py"
      via: "from extensions import db"
    - from: "web/models.py"
      to: "web/extensions.py"
      via: "from extensions import db"
---

<objective>
Umbau der Flask-App von globaler Instanz auf App Factory Pattern mit SQLAlchemy, Flask-Migrate und User-Modell.

Purpose: Fundament fuer alle v2.0-Features — ohne App Factory koennen weder Auth noch Blueprints noch DB sauber integriert werden.
Output: Funktionsfaehige App mit create_app(), User-Modell, initialen Migrationen, bestehende Routes als main-Blueprint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fundament-auth/04-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@web/app.py
@web/config.py
@web/formatting.py
@web/requirements.txt
@web/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extensions, Models und requirements.txt</name>
  <files>web/extensions.py, web/models.py, web/requirements.txt</files>
  <read_first>web/requirements.txt, web/app.py</read_first>
  <action>
1. Erstelle `web/extensions.py` mit folgenden Instanzen (OHNE App-Binding — das passiert in create_app):

```python
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate
from flask_login import LoginManager
from flask_wtf.csrf import CSRFProtect

db = SQLAlchemy()
migrate = Migrate()
login_manager = LoginManager()
csrf = CSRFProtect()

login_manager.login_view = 'auth.login'
login_manager.login_message = 'Bitte einloggen.'
login_manager.login_message_category = 'info'
```

2. Erstelle `web/models.py` mit User-Modell (per D: User-Entscheidungen aus CONTEXT.md — is_admin Flag, kein Rollen-System, Deaktivierung statt Loeschung):

```python
from flask_login import UserMixin
from werkzeug.security import generate_password_hash, check_password_hash
from datetime import datetime, timezone
from extensions import db

class User(UserMixin, db.Model):
    __tablename__ = 'users'

    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(64), unique=True, nullable=False, index=True)
    password_hash = db.Column(db.String(256), nullable=False)
    is_admin = db.Column(db.Boolean, default=False, nullable=False)
    is_active_user = db.Column(db.Boolean, default=True, nullable=False)
    created_at = db.Column(db.DateTime, default=lambda: datetime.now(timezone.utc))

    def set_password(self, password):
        self.password_hash = generate_password_hash(password)

    def check_password(self, password):
        return check_password_hash(self.password_hash, password)

    @property
    def is_active(self):
        return self.is_active_user

    def __repr__(self):
        return '<User {}>'.format(self.username)
```

Hinweis: `is_active` als Property ueberschreibt UserMixin.is_active — Flask-Login prueft damit automatisch ob User deaktiviert ist. Das DB-Feld heisst `is_active_user` um Konflikte mit der Property zu vermeiden.

3. Aktualisiere `web/requirements.txt` — bestehende Eintraege beibehalten, neue hinzufuegen:

```
Flask==2.3.3
pandas==2.3.1
openpyxl==3.1.5
Werkzeug==2.3.7
requests==2.31.0
pytz==2023.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.2
gunicorn==21.2.0
```

gunicorn wird fuer den spaeteren Produktions-Entrypoint benoetigt (aktuell laeuft `python app.py`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && source venv/bin/activate && pip install -r web/requirements.txt && python -c "from web.extensions import db, migrate, login_manager, csrf; print('extensions OK')" && python -c "from web.models import User; print('models OK')"</automated>
  </verify>
  <done>extensions.py exportiert db, migrate, login_manager, csrf. models.py exportiert User mit set_password/check_password. requirements.txt enthaelt alle neuen Dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: App Factory und Main Blueprint</name>
  <files>web/app.py, web/main/__init__.py, web/main/routes.py</files>
  <read_first>web/app.py, web/extensions.py, web/models.py, web/config.py, web/templates/base.html</read_first>
  <action>
1. Erstelle `web/main/__init__.py`:

```python
from flask import Blueprint

bp = Blueprint('main', __name__)

from main import routes  # noqa: E402, F401
```

2. Erstelle `web/main/routes.py` — ALLE bestehenden Routes aus app.py hierher verschieben:
- Ersetze `@app.route` durch `@bp.route`
- Ersetze `url_for('index')` durch `url_for('main.index')`, `url_for('upload_file')` durch `url_for('main.upload_file')`, etc.
- Importiere: `from flask import render_template, request, send_file, flash, redirect, url_for`
- Importiere: `from churchdesk_api import ChurchDeskAPI, EventAnalyzer, create_multi_org_client, extract_boyens_location`
- Importiere: `from formatting import format_date, format_time, format_service_type, format_pastor`
- Importiere: `from config import ORGANIZATIONS`
- Verschiebe ALLE Hilfsfunktionen mit: `format_parish_name`, `_extract_suffix`, `_build_location_entries`, `process_excel_file`, `convert_churchdesk_events_to_boyens`
- Registriere den Template-Filter im Blueprint: `@bp.app_template_filter('format_parish')` ueber `format_parish_name`

Routes die verschoben werden:
  - `index` → `bp.route('/')`
  - `upload_file` → `bp.route('/upload', methods=['POST'])`
  - `download_file` → `bp.route('/download', methods=['POST'])`
  - `fetch_churchdesk_events` → `bp.route('/fetch_churchdesk_events', methods=['POST'])`
  - `export_selected_events` → `bp.route('/export_selected_events', methods=['POST'])`

3. Schreibe `web/app.py` komplett neu als App Factory:

```python
import os
from flask import Flask


def create_app():
    app = Flask(__name__)

    # SECRET_KEY — MUSS gesetzt sein, kein Fallback (Pitfall 1)
    secret_key = os.getenv('SECRET_KEY')
    if not secret_key:
        raise RuntimeError(
            'SECRET_KEY nicht gesetzt. '
            'Generiere mit: python -c "import secrets; print(secrets.token_hex(32))" '
            'und setze als Environment Variable.'
        )
    app.config['SECRET_KEY'] = secret_key

    # Datenbank — SQLite in /app/data/ (Docker Volume)
    db_path = os.path.join(app.root_path, 'data', 'app.db')
    os.makedirs(os.path.dirname(db_path), exist_ok=True)
    app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + db_path
    app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False

    # Session-Konfiguration (30-Tage Remember Me)
    app.config['REMEMBER_COOKIE_DURATION'] = 30 * 24 * 3600  # 30 Tage in Sekunden

    # Extensions initialisieren
    from extensions import db, migrate, login_manager, csrf
    db.init_app(app)
    migrate.init_app(app, db)
    login_manager.init_app(app)
    csrf.init_app(app)

    # User-Loader fuer Flask-Login
    from models import User

    @login_manager.user_loader
    def load_user(user_id):
        return db.session.get(User, int(user_id))

    # Blueprints registrieren
    from main import bp as main_bp
    app.register_blueprint(main_bp)

    return app


# Fuer lokale Entwicklung: SECRET_KEY=dev python app.py
if __name__ == '__main__':
    app = create_app()
    app.run(debug=True, host='0.0.0.0', port=5000)
```

4. Aktualisiere `web/templates/base.html` — aendere `url_for('index')` zu `url_for('main.index')` in der Navbar.

5. Pruefe ALLE Templates auf url_for-Aufrufe und passe an:
   - `index.html`: `url_for('upload_file')` → `url_for('main.upload_file')`, `url_for('fetch_churchdesk_events')` → `url_for('main.fetch_churchdesk_events')`
   - `result.html`: `url_for('index')` → `url_for('main.index')`, `url_for('download_file')` → `url_for('main.download_file')`
   - `churchdesk_events.html`: `url_for('export_selected_events')` → `url_for('main.export_selected_events')`, `url_for('index')` → `url_for('main.index')`

6. Initialisiere Flask-Migrate (im web/-Verzeichnis):
```bash
cd web
SECRET_KEY=dev flask db init
SECRET_KEY=dev flask db migrate -m "Initial: User model"
```
Committe den `migrations/`-Ordner ins Repo (Pitfall 2 — migrations muessen im Repo sein, nicht im Container generiert).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine/web && SECRET_KEY=dev python -c "from app import create_app; app = create_app(); print('Factory OK')" && SECRET_KEY=dev flask db upgrade && SECRET_KEY=dev flask routes | grep -E "main\.(index|upload|download|fetch|export)"</automated>
  </verify>
  <done>create_app() erzeugt funktionierende App. Alle 5 bestehenden Routes sind unter main-Blueprint registriert. migrations/ existiert mit Initial-Migration. flask db upgrade laeuft fehlerfrei. SECRET_KEY='' loest RuntimeError aus.</done>
</task>

<task type="auto">
  <name>Task 3: Tests und Smoke-Verify</name>
  <files>tests/test_app_factory.py</files>
  <read_first>web/app.py, web/main/routes.py, tests/</read_first>
  <action>
Erstelle `tests/test_app_factory.py` mit Smoke-Tests fuer den App-Factory-Refactor:

```python
import pytest
import os

# SECRET_KEY fuer Tests setzen
os.environ['SECRET_KEY'] = 'test-secret-key-for-unit-tests'


def create_test_app():
    """Test-App mit temporaerer SQLite-DB."""
    from web.app import create_app
    app = create_app()
    app.config['TESTING'] = True
    app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite://'  # In-Memory
    app.config['WTF_CSRF_ENABLED'] = False
    from web.extensions import db
    with app.app_context():
        db.create_all()
    return app


class TestAppFactory:
    def test_create_app_returns_flask_instance(self):
        app = create_test_app()
        assert app is not None
        assert app.config['TESTING'] is True

    def test_secret_key_required(self):
        old = os.environ.pop('SECRET_KEY', None)
        try:
            from web.app import create_app
            with pytest.raises(RuntimeError, match='SECRET_KEY'):
                create_app()
        finally:
            if old:
                os.environ['SECRET_KEY'] = old

    def test_index_route_exists(self):
        app = create_test_app()
        with app.test_client() as client:
            resp = client.get('/')
            assert resp.status_code == 200

    def test_routes_use_main_blueprint(self):
        app = create_test_app()
        rules = [r.endpoint for r in app.url_map.iter_rules()]
        assert 'main.index' in rules
        assert 'main.upload_file' in rules
        assert 'main.download_file' in rules
        assert 'main.fetch_churchdesk_events' in rules
        assert 'main.export_selected_events' in rules


class TestUserModel:
    def test_set_and_check_password(self):
        from web.models import User
        user = User(username='test')
        user.set_password('geheim123')
        assert user.check_password('geheim123')
        assert not user.check_password('falsch')

    def test_is_active_reflects_is_active_user(self):
        from web.models import User
        user = User(username='test', is_active_user=True)
        assert user.is_active is True
        user.is_active_user = False
        assert user.is_active is False
```

Pruefe dass bestehende Tests weiterhin laufen (falls vorhanden):
```bash
cd /Users/simonluthe/Documents/kk-termine && source venv/bin/activate && python -m pytest tests/ -x -v
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && source venv/bin/activate && python -m pytest tests/test_app_factory.py -x -v</automated>
  </verify>
  <done>Alle App-Factory-Tests und User-Model-Tests bestehen. Bestehende Tests (falls vorhanden) sind nicht kaputt.</done>
</task>

</tasks>

<verification>
1. `SECRET_KEY=dev flask routes` zeigt alle 5 Routes unter main-Blueprint
2. `SECRET_KEY=dev flask db upgrade` laeuft fehlerfrei
3. `python -m pytest tests/test_app_factory.py -x -v` — alle Tests gruen
4. `SECRET_KEY='' python -c "from web.app import create_app; create_app()"` — RuntimeError
</verification>

<success_criteria>
- create_app() Factory Pattern funktioniert (FOUND-01)
- SQLAlchemy + Flask-Migrate initialisiert, User-Modell migriert (FOUND-02)
- SECRET_KEY RuntimeError ohne Fallback (FOUND-04)
- Alle bestehenden Routes funktionieren unter main-Blueprint
- migrations/ Ordner im Repo committed
</success_criteria>

<output>
After completion, create `.planning/phases/04-fundament-auth/04-01-SUMMARY.md`
</output>
