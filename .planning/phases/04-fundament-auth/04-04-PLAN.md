---
phase: 04-fundament-auth
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - web/admin/__init__.py
  - web/admin/routes.py
  - web/admin/forms.py
  - web/templates/admin/users.html
  - web/templates/admin/edit_user.html
  - web/templates/base.html
  - web/app.py
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "Admin kann neue User anlegen mit Benutzername und Passwort"
    - "Admin kann Passwort eines bestehenden Users zuruecksetzen"
    - "Admin kann User deaktivieren (nicht loeschen)"
    - "Erster Admin-User wird via flask create-admin CLI erstellt"
    - "Nicht-Admins haben keinen Zugang zu /admin/* Routes"
  artifacts:
    - path: "web/admin/routes.py"
      provides: "Admin-Routes fuer User-Verwaltung"
      contains: "@admin_required"
    - path: "web/admin/forms.py"
      provides: "CreateUserForm, EditUserForm"
      contains: "class CreateUserForm"
    - path: "web/templates/admin/users.html"
      provides: "User-Liste"
    - path: "web/templates/admin/edit_user.html"
      provides: "User bearbeiten"
  key_links:
    - from: "web/admin/routes.py"
      to: "web/models.py"
      via: "User CRUD"
      pattern: "User\\.query|db\\.session\\.add"
    - from: "web/app.py"
      to: "web/admin/"
      via: "Blueprint-Registrierung"
      pattern: "register_blueprint.*admin"
---

<objective>
Admin-User-Verwaltung: User anlegen, bearbeiten, deaktivieren. CLI-Befehl fuer ersten Admin-User.

Purpose: Admin muss neue User anlegen koennen ohne Server-Zugriff (ausser fuer den allerersten Admin).
Output: /admin/users Route, flask create-admin CLI, User-CRUD.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fundament-auth/04-CONTEXT.md
@.planning/phases/04-fundament-auth/04-01-SUMMARY.md
@.planning/phases/04-fundament-auth/04-02-SUMMARY.md

@web/app.py
@web/models.py
@web/extensions.py
@web/auth/routes.py
@web/templates/base.html

<interfaces>
From web/models.py:
```python
class User(UserMixin, db.Model):
    id: int (PK)
    username: str (unique)
    password_hash: str
    is_admin: bool
    is_active_user: bool
    created_at: datetime

    def set_password(self, password): ...
    def check_password(self, password) -> bool: ...
    @property
    def is_active(self) -> bool: ...
```

From web/extensions.py:
```python
db = SQLAlchemy()
login_manager = LoginManager()
csrf = CSRFProtect()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Blueprint mit User-CRUD</name>
  <files>web/admin/__init__.py, web/admin/routes.py, web/admin/forms.py, web/templates/admin/users.html, web/templates/admin/edit_user.html</files>
  <read_first>web/models.py, web/extensions.py, web/auth/routes.py, web/templates/base.html</read_first>
  <action>
1. Erstelle `web/admin/__init__.py`:

```python
from flask import Blueprint

bp = Blueprint('admin', __name__, url_prefix='/admin')

from admin import routes  # noqa: E402, F401
```

2. Erstelle `web/admin/forms.py`:

```python
from flask_wtf import FlaskForm
from wtforms import StringField, PasswordField, BooleanField, SubmitField
from wtforms.validators import DataRequired, Length, ValidationError
from models import User


class CreateUserForm(FlaskForm):
    username = StringField('Benutzername', validators=[DataRequired(), Length(min=3, max=64)])
    password = PasswordField('Passwort', validators=[DataRequired(), Length(min=8, message='Mindestens 8 Zeichen')])
    is_admin = BooleanField('Admin-Rechte')
    submit = SubmitField('Benutzer anlegen')

    def validate_username(self, field):
        if User.query.filter_by(username=field.data).first():
            raise ValidationError('Benutzername bereits vergeben.')


class EditUserForm(FlaskForm):
    password = PasswordField('Neues Passwort (leer lassen um nicht zu aendern)', validators=[Length(min=8, message='Mindestens 8 Zeichen')])
    is_admin = BooleanField('Admin-Rechte')
    is_active = BooleanField('Aktiv')
    submit = SubmitField('Speichern')

    def validate_password(self, field):
        # Leeres Passwort ist OK (= nicht aendern), aber wenn gesetzt dann min 8
        if field.data and len(field.data) < 8:
            raise ValidationError('Mindestens 8 Zeichen')
```

Hinweis: EditUserForm.password hat Length-Validator, aber da das Feld optional ist (leer lassen = nicht aendern), muss der custom validate_password die leere Eingabe erlauben. Setze `validators=[]` auf das PasswordField und validiere nur im custom Validator:

```python
password = PasswordField('Neues Passwort (leer lassen um nicht zu aendern)')
```

3. Erstelle `web/admin/routes.py`:

```python
from functools import wraps
from flask import render_template, redirect, url_for, flash, abort
from flask_login import login_required, current_user
from admin import bp
from models import User
from extensions import db
from admin.forms import CreateUserForm, EditUserForm


def admin_required(f):
    """Decorator: login_required + is_admin Check."""
    @wraps(f)
    @login_required
    def decorated(*args, **kwargs):
        if not current_user.is_admin:
            abort(403)
        return f(*args, **kwargs)
    return decorated


@bp.route('/users')
@admin_required
def users():
    all_users = User.query.order_by(User.username).all()
    return render_template('admin/users.html', users=all_users)


@bp.route('/users/new', methods=['GET', 'POST'])
@admin_required
def create_user():
    form = CreateUserForm()
    if form.validate_on_submit():
        user = User(username=form.username.data, is_admin=form.is_admin.data)
        user.set_password(form.password.data)
        db.session.add(user)
        db.session.commit()
        flash('Benutzer "{}" angelegt.'.format(user.username), 'success')
        return redirect(url_for('admin.users'))
    return render_template('admin/edit_user.html', form=form, title='Neuer Benutzer', is_new=True)


@bp.route('/users/<int:user_id>/edit', methods=['GET', 'POST'])
@admin_required
def edit_user(user_id):
    user = db.session.get(User, user_id)
    if user is None:
        abort(404)
    form = EditUserForm(obj=user)
    # is_active Feld auf is_active_user mappen
    if request.method == 'GET':
        form.is_active.data = user.is_active_user
    if form.validate_on_submit():
        if form.password.data:
            user.set_password(form.password.data)
        user.is_admin = form.is_admin.data
        user.is_active_user = form.is_active.data
        db.session.commit()
        flash('Benutzer "{}" aktualisiert.'.format(user.username), 'success')
        return redirect(url_for('admin.users'))
    return render_template('admin/edit_user.html', form=form, user=user, title='Benutzer bearbeiten', is_new=False)
```

WICHTIG: `from flask import request` muss im Import stehen fuer den GET-Check.

4. Erstelle `web/templates/admin/users.html`:

```html
{% extends "base.html" %}

{% block title %}Benutzer verwalten{% endblock %}

{% block content %}
<div class="row mt-4">
    <div class="col-md-8 offset-md-2">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2>Benutzer</h2>
            <a href="{{ url_for('admin.create_user') }}" class="btn btn-primary">Neuer Benutzer</a>
        </div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Benutzername</th>
                    <th>Admin</th>
                    <th>Aktiv</th>
                    <th>Erstellt</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                <tr>
                    <td>{{ user.username }}</td>
                    <td>{% if user.is_admin %}Ja{% else %}Nein{% endif %}</td>
                    <td>{% if user.is_active_user %}Ja{% else %}Nein{% endif %}</td>
                    <td>{{ user.created_at.strftime('%d.%m.%Y') if user.created_at else '-' }}</td>
                    <td><a href="{{ url_for('admin.edit_user', user_id=user.id) }}" class="btn btn-sm btn-outline-secondary">Bearbeiten</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
```

5. Erstelle `web/templates/admin/edit_user.html`:

```html
{% extends "base.html" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<div class="row justify-content-center mt-4">
    <div class="col-md-6">
        <h2>{{ title }}</h2>
        {% if not is_new %}
        <p class="text-muted">Benutzer: {{ user.username }}</p>
        {% endif %}
        <form method="POST">
            {{ form.hidden_tag() }}
            {% if is_new %}
            <div class="mb-3">
                {{ form.username.label(class="form-label") }}
                {{ form.username(class="form-control") }}
                {% for error in form.username.errors %}
                <div class="text-danger">{{ error }}</div>
                {% endfor %}
            </div>
            {% endif %}
            <div class="mb-3">
                {{ form.password.label(class="form-label") }}
                {{ form.password(class="form-control") }}
                {% for error in form.password.errors %}
                <div class="text-danger">{{ error }}</div>
                {% endfor %}
            </div>
            <div class="mb-3 form-check">
                {{ form.is_admin(class="form-check-input") }}
                {{ form.is_admin.label(class="form-check-label") }}
            </div>
            {% if not is_new %}
            <div class="mb-3 form-check">
                {{ form.is_active(class="form-check-input") }}
                {{ form.is_active.label(class="form-check-label") }}
            </div>
            {% endif %}
            <div class="d-flex gap-2">
                {{ form.submit(class="btn btn-primary") }}
                <a href="{{ url_for('admin.users') }}" class="btn btn-secondary">Abbrechen</a>
            </div>
        </form>
    </div>
</div>
{% endblock %}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine/web && test -f admin/__init__.py && test -f admin/routes.py && test -f admin/forms.py && test -f templates/admin/users.html && test -f templates/admin/edit_user.html && echo "Admin blueprint files OK"</automated>
  </verify>
  <done>Admin-Blueprint existiert mit User-Liste, User anlegen, User bearbeiten. admin_required Decorator prueft is_admin. Templates nutzen CSRF via hidden_tag().</done>
</task>

<task type="auto">
  <name>Task 2: CLI-Befehl, Blueprint-Registrierung und Admin-Link in Navbar</name>
  <files>web/app.py, web/templates/base.html, tests/test_admin.py</files>
  <read_first>web/app.py, web/templates/base.html</read_first>
  <action>
1. Ergaenze `web/app.py` in create_app() — Admin-Blueprint registrieren (nach auth-Blueprint):

```python
from admin import bp as admin_bp
app.register_blueprint(admin_bp)
```

2. Fuege CLI-Befehl `create-admin` in create_app() hinzu (nach Blueprint-Registrierung):

```python
import click

@app.cli.command('create-admin')
@click.argument('username')
@click.password_option()
def create_admin_command(username, password):
    """Erstellt einen Admin-Benutzer."""
    from models import User
    if User.query.filter_by(username=username).first():
        click.echo('Fehler: Benutzer "{}" existiert bereits.'.format(username))
        raise SystemExit(1)
    if len(password) < 8:
        click.echo('Fehler: Passwort muss mindestens 8 Zeichen haben.')
        raise SystemExit(1)
    user = User(username=username, is_admin=True)
    user.set_password(password)
    db.session.add(user)
    db.session.commit()
    click.echo('Admin-Benutzer "{}" erstellt.'.format(username))
```

Hinweis: `db` muss bereits importiert sein (from extensions import db) — das ist es bereits im bestehenden create_app().

3. Ergaenze `web/templates/base.html` — Admin-Link in der Navbar (nur fuer Admins sichtbar). Im Navbar-Bereich nach dem Logout-Link:

```html
{% if current_user.is_admin %}
<a class="nav-link" href="{{ url_for('admin.users') }}">Benutzer</a>
{% endif %}
```

4. Erstelle `tests/test_admin.py`:

```python
import pytest
import os

os.environ['SECRET_KEY'] = 'test-secret-key-for-unit-tests'


def create_test_app():
    from web.app import create_app
    app = create_app()
    app.config['TESTING'] = True
    app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite://'
    app.config['WTF_CSRF_ENABLED'] = False
    from web.extensions import db
    with app.app_context():
        db.create_all()
    return app


def login(client, username, password):
    return client.post('/login', data={
        'username': username,
        'password': password,
    }, follow_redirects=True)


def create_user(app, username, password, is_admin=False):
    from web.extensions import db
    from web.models import User
    with app.app_context():
        user = User(username=username, is_admin=is_admin)
        user.set_password(password)
        db.session.add(user)
        db.session.commit()


class TestAdminAccess:
    @pytest.fixture(autouse=True)
    def setup(self):
        self.app = create_test_app()
        self.client = self.app.test_client()
        create_user(self.app, 'admin', 'adminpass1', is_admin=True)
        create_user(self.app, 'normal', 'normalpass1', is_admin=False)

    def test_admin_can_access_users(self):
        login(self.client, 'admin', 'adminpass1')
        resp = self.client.get('/admin/users')
        assert resp.status_code == 200
        assert b'Benutzer' in resp.data

    def test_normal_user_gets_403(self):
        login(self.client, 'normal', 'normalpass1')
        resp = self.client.get('/admin/users')
        assert resp.status_code == 403

    def test_anonymous_gets_redirect(self):
        resp = self.client.get('/admin/users')
        assert resp.status_code == 302
        assert '/login' in resp.headers['Location']

    def test_admin_can_create_user(self):
        login(self.client, 'admin', 'adminpass1')
        resp = self.client.post('/admin/users/new', data={
            'username': 'newuser',
            'password': 'newpass12',
            'is_admin': False,
        }, follow_redirects=True)
        assert resp.status_code == 200
        assert b'newuser' in resp.data

    def test_admin_can_deactivate_user(self):
        login(self.client, 'admin', 'adminpass1')
        from web.models import User
        with self.app.app_context():
            user = User.query.filter_by(username='normal').first()
            user_id = user.id
        resp = self.client.post('/admin/users/{}/edit'.format(user_id), data={
            'is_admin': False,
            'is_active': False,
        }, follow_redirects=True)
        assert resp.status_code == 200
        # Verify user is deactivated
        with self.app.app_context():
            user = User.query.filter_by(username='normal').first()
            assert user.is_active_user is False


class TestCreateAdminCLI:
    def test_create_admin_command(self):
        app = create_test_app()
        runner = app.test_cli_runner()
        result = runner.invoke(args=['create-admin', 'cliuser', '--password', 'clipass12'])
        assert 'erstellt' in result.output
        from web.models import User
        with app.app_context():
            user = User.query.filter_by(username='cliuser').first()
            assert user is not None
            assert user.is_admin is True
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && source venv/bin/activate && python -m pytest tests/test_admin.py -x -v</automated>
  </verify>
  <done>Admin-Blueprint registriert. flask create-admin CLI funktioniert. Admin kann User anlegen/bearbeiten/deaktivieren. Nicht-Admins bekommen 403. Alle Tests bestehen.</done>
</task>

</tasks>

<verification>
1. `SECRET_KEY=dev flask create-admin testadmin --password testpass12` → "Admin-Benutzer erstellt"
2. `python -m pytest tests/test_admin.py -x -v` → alle gruen
3. Admin sieht "Benutzer"-Link in Navbar, normaler User nicht
</verification>

<success_criteria>
- Admin kann neue User anlegen und deren Passwoerter setzen (AUTH-04)
- User werden deaktiviert statt geloescht (per CONTEXT.md Entscheidung)
- Erster Admin via flask create-admin CLI
- Nicht-Admins haben keinen Zugang zu Admin-Routes
</success_criteria>

<output>
After completion, create `.planning/phases/04-fundament-auth/04-04-SUMMARY.md`
</output>
