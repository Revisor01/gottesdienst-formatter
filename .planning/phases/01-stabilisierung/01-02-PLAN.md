---
phase: 01-stabilisierung
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - web/churchdesk_api.py
  - web/docker-compose.yml
  - web/templates/index.html
  - gottesdienst_formatter_final.py
  - .env.example
autonomous: true
requirements:
  - SEC-01
  - CODE-03
  - CODE-04

must_haves:
  truths:
    - "web/churchdesk_api.py enthaelt keine hardcoded API-Tokens mehr"
    - "web/churchdesk_api.py importiert ORGANIZATIONS aus config.py und format_pastor aus formatting.py"
    - "docker-compose.yml hat keine hardcoded Token-Fallbacks (kein :-Syntax mit Token-Werten)"
    - "index.html rendert Org-Checkboxen dynamisch aus der organizations Template-Variable"
    - "Eine neue Organisation erfordert nur neue Env-Vars — keinen Code-Change"
    - "gottesdienst_formatter_final.py importiert aus web/formatting.py statt eigener Kopien"
  artifacts:
    - path: "web/churchdesk_api.py"
      provides: "API-Client ohne hardcoded Tokens"
      contains: "from config import ORGANIZATIONS"
    - path: "web/docker-compose.yml"
      provides: "Env-Var-basierte Konfiguration"
      contains: "CHURCHDESK_ORG_IDS"
    - path: "web/templates/index.html"
      provides: "Dynamische Org-Checkboxen"
      contains: "for org_id, org in organizations.items()"
    - path: ".env.example"
      provides: "Dokumentation der benoetigten Env-Vars"
      contains: "CHURCHDESK_ORG_IDS"
  key_links:
    - from: "web/churchdesk_api.py"
      to: "web/config.py"
      via: "from config import ORGANIZATIONS"
      pattern: "from config import ORGANIZATIONS"
    - from: "web/churchdesk_api.py"
      to: "web/formatting.py"
      via: "from formatting import format_pastor"
      pattern: "from formatting import format_pastor"
    - from: "web/docker-compose.yml"
      to: "web/config.py"
      via: "CHURCHDESK_ORG_* Env-Vars"
      pattern: "CHURCHDESK_ORG_IDS"
    - from: "gottesdienst_formatter_final.py"
      to: "web/formatting.py"
      via: "sys.path insert + import"
      pattern: "from formatting import"
---

<objective>
Alle verbleibenden Consumer auf die neuen Module umstellen und hardcoded Credentials entfernen.

Purpose: Nach Plan 01 existieren formatting.py und config.py, aber churchdesk_api.py hat noch die alten ORGANIZATIONS mit Tokens, docker-compose.yml hat noch Fallback-Tokens, index.html hat hardcoded Checkboxen, und das Standalone-Script hat noch eigene Formatierungskopien. Dieser Plan schliesst die Migration ab.

Output: Vollstaendig bereinigte Codebasis — keine hardcoded Tokens, keine duplizierten Funktionen, dynamische Org-Verwaltung, .env.example als Dokumentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md
@.planning/phases/01-stabilisierung/01-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 erstellte Module: -->

web/formatting.py exportiert:
  - format_date(date_obj) -> str
  - format_time(date_obj) -> str
  - format_service_type(titel) -> str
  - format_pastor(contributor: str) -> str

web/config.py exportiert:
  - load_organizations() -> dict
  - ORGANIZATIONS (dict, geladen aus Env-Vars)

<!-- Aktuelle churchdesk_api.py Imports/Exports die andere Module nutzen: -->
app.py importiert: ChurchDeskAPI, EventAnalyzer, create_multi_org_client, extract_boyens_location
(format_boyens_pastor und ORGANIZATIONS kommen jetzt aus formatting.py / config.py — per Plan 01)

<!-- churchdesk_api.py interne Nutzung von ORGANIZATIONS: -->
Z. 428-448: MultiOrganizationChurchDeskAPI.__init__() nutzt ORGANIZATIONS
Z. 514-537: create_api_client() nutzt ORGANIZATIONS
Z. 540-542: create_multi_org_client() erstellt MultiOrganizationChurchDeskAPI

<!-- churchdesk_api.py interne Nutzung von format_boyens_pastor: -->
Z. 236 (ca.): EventAnalyzer.format_event_for_boyens() ruft format_boyens_pastor() auf
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: churchdesk_api.py bereinigen — ORGANIZATIONS und format_boyens_pastor entfernen, Imports umstellen</name>
  <files>web/churchdesk_api.py</files>
  <read_first>
    - web/churchdesk_api.py (vollstaendig — ORGANIZATIONS Z. 15-41, format_boyens_pastor Z. 358-422, alle Stellen die diese nutzen)
    - web/config.py (erstellt in Plan 01 — Import-Pfad fuer ORGANIZATIONS)
    - web/formatting.py (erstellt in Plan 01 — Import-Pfad fuer format_pastor)
  </read_first>
  <action>
web/churchdesk_api.py aendern:

1. LOESCHEN: Das gesamte ORGANIZATIONS-Dict (aktuell Z. 15-41) — alle 5 Eintraege mit ihren hardcoded Tokens.

2. NEUE IMPORTS am Dateianfang (nach den bestehenden stdlib-Imports):
   ```python
   from config import ORGANIZATIONS
   from formatting import format_pastor
   ```

3. ALLE Aufrufe von `format_boyens_pastor(...)` in der Datei ersetzen durch `format_pastor(...)`. Suche nach `format_boyens_pastor` und ersetze durch `format_pastor`.

4. LOESCHEN: Die gesamte `def format_boyens_pastor(...)` Funktionsdefinition (aktuell Z. 358-422).

5. create_api_client() (aktuell Z. 514-537) — Fallback-Logik aendern:
   VORHER (Z. 519-521):
   ```python
   if not api_token:
       # Try to use default token for org 2596
       api_token = ORGANIZATIONS.get(2596, {}).get('token')
   ```
   NACHHER:
   ```python
   if not api_token:
       api_token = ORGANIZATIONS.get(2596, {}).get('token')
   ```
   (Kommentar entfernen der "default token" suggeriert — der Token kommt jetzt aus Env-Vars, nicht mehr hardcoded.)

6. SICHERSTELLEN dass `ORGANIZATIONS` weiterhin von churchdesk_api.py exportiert wird (fuer Rueckwaertskompatibilitaet mit externen Imports). Da es jetzt per `from config import ORGANIZATIONS` importiert wird, ist es automatisch im Modul-Namespace verfuegbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && grep -c "d4ec66780546786c92b916f873ee713181c1b695d32e7ba9839e760eaecd3fa1\|7b0cf910b378c6d2482419f4e785fc95\|c2d76c9414f6aac773c1643a98131123\|3afe57b4ae54ece02ff568993777028b\|bZq4GLCvhUbkYFQrDVDAe3cTs8hVlyQq" web/churchdesk_api.py</automated>
  </verify>
  <acceptance_criteria>
    - web/churchdesk_api.py enthaelt `from config import ORGANIZATIONS`
    - web/churchdesk_api.py enthaelt `from formatting import format_pastor`
    - web/churchdesk_api.py enthaelt KEIN `ORGANIZATIONS = {` (eigenes Dict)
    - web/churchdesk_api.py enthaelt NICHT den String `d4ec66780546786c92b916f873ee713181c1b695d32e7ba9839e760eaecd3fa1` (oder einen der anderen 4 Tokens)
    - web/churchdesk_api.py enthaelt KEINE `def format_boyens_pastor(` Definition
    - web/churchdesk_api.py enthaelt NICHT den String `format_boyens_pastor` (weder Definition noch Aufruf)
    - grep nach `format_pastor` in churchdesk_api.py findet mindestens 2 Treffer (Import + Aufruf)
  </acceptance_criteria>
  <done>churchdesk_api.py hat keine hardcoded Tokens, importiert ORGANIZATIONS aus config.py und format_pastor aus formatting.py</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml, index.html, .env.example und Standalone-Script aktualisieren</name>
  <files>web/docker-compose.yml, web/templates/index.html, .env.example, gottesdienst_formatter_final.py</files>
  <read_first>
    - web/docker-compose.yml (vollstaendig — aktuelle Env-Var-Konfiguration mit hardcoded Fallback Z. 15)
    - web/templates/index.html (vollstaendig — hardcoded Org-Checkboxen)
    - gottesdienst_formatter_final.py (Zeilen 1-120 — Imports und format_*-Funktionsdefinitionen die ersetzt werden muessen)
    - web/config.py (erstellt in Plan 01 — CHURCHDESK_ORG_IDS Format verstehen)
  </read_first>
  <action>
VIER Dateien aendern:

### 1. web/docker-compose.yml

Environment-Block ersetzen. VORHER:
```yaml
environment:
  - FLASK_ENV=production
  - FLASK_DEBUG=false
  - CHURCHDESK_API_TOKEN=${CHURCHDESK_API_TOKEN:-d4ec66780546786c92b916f873ee713181c1b695d32e7ba9839e760eaecd3fa1}
  - CHURCHDESK_ORGANIZATION_ID=${CHURCHDESK_ORGANIZATION_ID:-2596}
```

NACHHER:
```yaml
environment:
  - FLASK_ENV=production
  - FLASK_DEBUG=false
  - SECRET_KEY=${SECRET_KEY}
  - CHURCHDESK_ORG_IDS=${CHURCHDESK_ORG_IDS}
  - CHURCHDESK_ORG_2596_NAME=${CHURCHDESK_ORG_2596_NAME}
  - CHURCHDESK_ORG_2596_TOKEN=${CHURCHDESK_ORG_2596_TOKEN}
  - CHURCHDESK_ORG_6572_NAME=${CHURCHDESK_ORG_6572_NAME}
  - CHURCHDESK_ORG_6572_TOKEN=${CHURCHDESK_ORG_6572_TOKEN}
  - CHURCHDESK_ORG_2719_NAME=${CHURCHDESK_ORG_2719_NAME}
  - CHURCHDESK_ORG_2719_TOKEN=${CHURCHDESK_ORG_2719_TOKEN}
  - CHURCHDESK_ORG_2725_NAME=${CHURCHDESK_ORG_2725_NAME}
  - CHURCHDESK_ORG_2725_TOKEN=${CHURCHDESK_ORG_2725_TOKEN}
  - CHURCHDESK_ORG_2729_NAME=${CHURCHDESK_ORG_2729_NAME}
  - CHURCHDESK_ORG_2729_TOKEN=${CHURCHDESK_ORG_2729_TOKEN}
  - CHURCHDESK_ORG_2596_DESCRIPTION=${CHURCHDESK_ORG_2596_DESCRIPTION:-}
  - CHURCHDESK_ORG_6572_DESCRIPTION=${CHURCHDESK_ORG_6572_DESCRIPTION:-}
  - CHURCHDESK_ORG_2719_DESCRIPTION=${CHURCHDESK_ORG_2719_DESCRIPTION:-}
  - CHURCHDESK_ORG_2725_DESCRIPTION=${CHURCHDESK_ORG_2725_DESCRIPTION:-}
  - CHURCHDESK_ORG_2729_DESCRIPTION=${CHURCHDESK_ORG_2729_DESCRIPTION:-}
```

WICHTIG: Keine `:-` Fallbacks auf echte Token-Werte. Nur Descriptions haben leere Fallbacks (das ist OK — Descriptions sind nicht geheim).

### 2. web/templates/index.html

Den hardcoded Org-Checkbox-Block (aktuell die zwei `<div class="row">` Bloecke mit den 4 Checkboxen fuer Org 2596, 6572, 2725, 2729) ersetzen durch dynamisches Jinja2-Template:

```html
<div class="mb-3">
    <label class="form-label">Kirchspiele auswaehlen:</label>
    {% if organizations %}
    <div class="row">
        {% for org_id, org in organizations.items() %}
        <div class="col-md-6 {% if not loop.first and loop.index0 % 2 == 0 %}mt-2{% endif %}">
            <div class="form-check">
                <input class="form-check-input org-checkbox" type="checkbox" name="selected_organizations" value="{{ org_id }}" id="org-{{ org_id }}" {% if org_id != 2596 %}checked{% endif %}>
                <label class="form-check-label" for="org-{{ org_id }}">
                    <strong>{{ org.name }}</strong><br>
                    {% if org.description %}<small class="text-muted">{{ org.description }}</small>{% endif %}
                </label>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="alert alert-warning">
        Keine Organisationen konfiguriert. Bitte CHURCHDESK_ORG_IDS und zugehoerige Env-Vars setzen.
    </div>
    {% endif %}
</div>
```

Org 2596 (Kirchenkreis Dithmarschen) ist standardmaessig NICHT gecheckt (wie im aktuellen Template), alle anderen sind gecheckt. Die Select-All-Checkbox und das JavaScript am Ende der Datei BEIBEHALTEN.

### 3. .env.example (NEUE Datei im Projekt-Root)

```
# Gottesdienst-Formatter Environment Variables
# Kopiere diese Datei nach .env und fulle die Werte aus.

# Flask
SECRET_KEY=hier-einen-sicheren-key-generieren

# ChurchDesk Organisationen
# Kommaseparierte Liste der Org-IDs
CHURCHDESK_ORG_IDS=2596,6572,2719,2725,2729

# Pro Organisation: NAME, TOKEN, DESCRIPTION (optional)
CHURCHDESK_ORG_2596_NAME=Kirchenkreis Dithmarschen
CHURCHDESK_ORG_2596_TOKEN=
CHURCHDESK_ORG_2596_DESCRIPTION=Zentrale Verwaltung

CHURCHDESK_ORG_6572_NAME=Kirchspiel Heide
CHURCHDESK_ORG_6572_TOKEN=
CHURCHDESK_ORG_6572_DESCRIPTION=Heide (St.-Juergen, Erloeser, Auferstehung), Nordhastedt, Wesseln, Hemmingstedt, Eddelak

CHURCHDESK_ORG_2719_NAME=KG Hennstedt (alt)
CHURCHDESK_ORG_2719_TOKEN=
CHURCHDESK_ORG_2719_DESCRIPTION=Nur Hennstedt Hauptkirche

CHURCHDESK_ORG_2725_NAME=Kirchspiel Eider
CHURCHDESK_ORG_2725_TOKEN=
CHURCHDESK_ORG_2725_DESCRIPTION=Hennstedt, Lunden, Hemme, St. Annen, Schlichting, Weddingstedt, Ostrohe, Stelle-Wittenwurth

CHURCHDESK_ORG_2729_NAME=Kirchspiel West
CHURCHDESK_ORG_2729_TOKEN=
CHURCHDESK_ORG_2729_DESCRIPTION=Buesum, Neuenkirchen, Wesselburen, Urlauberseelsorge
```

### 4. gottesdienst_formatter_final.py

Am Anfang der Datei (nach dem Shebang und Encoding-Zeile, vor allen anderen Imports) einfuegen:

```python
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), 'web'))
from formatting import format_date, format_time, format_service_type, format_pastor
```

Dann die lokalen Kopien der format_*-Funktionen LOESCHEN:
- `def format_date(date_obj):` und ihr gesamter Body
- `def format_time(date_obj):` und ihr gesamter Body
- `def format_service_type(titel):` und ihr gesamter Body
- `def format_pastor(mitwirkender):` und ihr gesamter Body

Die restliche Logik des Scripts (Excel-Verarbeitung, main, etc.) BEIBEHALTEN.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && grep -c "d4ec66780546786c92b916f873ee713181c1b695d32e7ba9839e760eaecd3fa1" web/docker-compose.yml && echo "FAIL: hardcoded token still in docker-compose.yml" || echo "OK: no hardcoded tokens in docker-compose.yml"</automated>
  </verify>
  <acceptance_criteria>
    - web/docker-compose.yml enthaelt `CHURCHDESK_ORG_IDS=` (Env-Var-Referenz)
    - web/docker-compose.yml enthaelt `SECRET_KEY=` (Env-Var-Referenz)
    - web/docker-compose.yml enthaelt NICHT den String `d4ec66780546786c92b916f873ee713181c1b695d32e7ba9839e760eaecd3fa1`
    - web/docker-compose.yml enthaelt NICHT `:-d4ec` oder andere Token-Fallbacks
    - web/templates/index.html enthaelt `{% for org_id, org in organizations.items() %}`
    - web/templates/index.html enthaelt `{% if not organizations %}` oder `{% else %}` mit Warnmeldung
    - web/templates/index.html enthaelt NICHT `value="2596"` als hardcoded Checkbox (dynamisch generiert ist OK)
    - .env.example existiert und enthaelt `CHURCHDESK_ORG_IDS=`
    - .env.example enthaelt KEINEN echten Token-Wert (alle TOKEN= Zeilen sind leer)
    - gottesdienst_formatter_final.py enthaelt `from formatting import`
    - gottesdienst_formatter_final.py enthaelt `sys.path.insert`
    - gottesdienst_formatter_final.py enthaelt KEINE `def format_date(` Definition
    - gottesdienst_formatter_final.py enthaelt KEINE `def format_pastor(` Definition
  </acceptance_criteria>
  <done>Alle Consumer nutzen die neuen Module, keine hardcoded Tokens in der gesamten Codebasis, dynamische Org-Liste im Template, .env.example dokumentiert benoetigte Env-Vars</done>
</task>

</tasks>

<verification>
Gesamtpruefung nach Plan 02:

1. Keine hardcoded Tokens in der gesamten Codebasis:
   `grep -r "d4ec66780546786c92b916f873ee713181c1b695d32e7ba9839e760eaecd3fa1\|7b0cf910b378c6d2482419f4e785fc95\|c2d76c9414f6aac773c1643a98131123\|3afe57b4ae54ece02ff568993777028b\|bZq4GLCvhUbkYFQrDVDAe3cTs8hVlyQq" web/ gottesdienst_formatter_final.py` gibt keine Treffer

2. Formatierungsfunktionen existieren genau einmal:
   `grep -r "def format_date\|def format_time\|def format_service_type\|def format_pastor" web/ gottesdienst_formatter_final.py` gibt genau 4 Treffer, alle in web/formatting.py

3. ORGANIZATIONS ist kein hardcoded Dict mehr:
   `grep -c "ORGANIZATIONS = {" web/churchdesk_api.py` gibt 0

4. docker-compose.yml hat keine Token-Fallbacks:
   `grep ":-" web/docker-compose.yml | grep -v "DESCRIPTION"` gibt keine Treffer (Descriptions duerfen leere Fallbacks haben)

5. .env.example existiert mit allen benoetigten Vars:
   `grep -c "CHURCHDESK_ORG_.*_TOKEN=" .env.example` gibt 5
</verification>

<success_criteria>
1. Kein API-Token oder Secret Key ist im Quellcode sichtbar — alle Credentials kommen aus Environment Variables
2. Formatierungslogik existiert in einem zentralen Modul (web/formatting.py), das von allen Codepfaden importiert wird
3. Eine neue ChurchDesk-Organisation wird durch Hinzufuegen von Env-Vars eingebunden — kein Code-Change noetig
4. Es gibt eine einzige format_pastor()-Funktion in formatting.py — die drei bisherigen Varianten sind entfernt
5. .env.example dokumentiert alle benoetigten Environment Variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilisierung/01-02-SUMMARY.md`
</output>
