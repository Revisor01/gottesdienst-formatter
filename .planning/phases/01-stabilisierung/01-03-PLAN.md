---
phase: 01-stabilisierung
plan: 03
type: execute
wave: 1
depends_on: ["01-01", "01-02"]
files_modified:
  - gottesdienst_formatter.py
  - .env.example
autonomous: true
gap_closure: true
requirements:
  - SEC-01
  - CODE-01

must_haves:
  truths:
    - "gottesdienst_formatter.py (der veraltete Prototyp) existiert nicht mehr im Projekt"
    - ".env.example enthaelt einen Hinweis zur Token-Rotation wegen Git-History-Kompromittierung"
  artifacts:
    - path: ".env.example"
      provides: "Token-Rotations-Hinweis"
      contains: "WICHTIG"
  key_links: []
---

<objective>
Gap Closure fuer Phase 01: Zwei offene Punkte aus der Verifikation schliessen.

1. SEC-01: Token-Rotation dokumentieren — die Tokens wurden aus dem Code entfernt, sind aber in der Git-History sichtbar. Da git filter-repo/force-push destruktiv ist, ist die pragmatische Loesung: Token-Rotation bei ChurchDesk. Dies muss als menschliche Aktion dokumentiert werden.

2. CODE-01: Veralteten Prototyp gottesdienst_formatter.py loeschen — die Datei enthaelt duplizierte Formatierungslogik und wird nicht produktiv genutzt. gottesdienst_formatter_final.py ist die aktive Version.

Purpose: Verifikations-Gaps schliessen, damit Phase 01 vollstaendig abgeschlossen ist.
Output: Bereinigte Codebasis ohne toten Prototyp, dokumentierte Token-Rotations-Anforderung.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Veralteten Prototyp gottesdienst_formatter.py loeschen</name>
  <files>gottesdienst_formatter.py</files>
  <read_first>gottesdienst_formatter.py, gottesdienst_formatter_final.py</read_first>
  <action>
Die Datei `gottesdienst_formatter.py` im Projekt-Root loeschen (per `git rm`).

Diese Datei ist ein veralteter Prototyp, der eigene format_date() und format_time() Definitionen enthaelt (Zeilen 22 und 49), die NICHT aus web/formatting.py importiert werden. Die aktive Version ist `gottesdienst_formatter_final.py`, die bereits korrekt aus web/formatting.py importiert.

Durch das Loeschen wird CODE-01 ("Formatierungslogik in einem zentralen Modul") vollstaendig erfuellt — es gibt dann keine duplizierte Formatierungslogik mehr ausserhalb von web/formatting.py.

Schritt:
1. `git rm gottesdienst_formatter.py` ausfuehren
  </action>
  <verify>
    <automated>test ! -f /Users/simonluthe/Documents/kk-termine/gottesdienst_formatter.py && echo "DELETED" || echo "STILL EXISTS"</automated>
  </verify>
  <acceptance_criteria>gottesdienst_formatter.py existiert nicht mehr im Working Tree. gottesdienst_formatter_final.py existiert weiterhin und importiert aus web/formatting.py.</acceptance_criteria>
  <done>gottesdienst_formatter.py ist geloescht. Nur noch gottesdienst_formatter_final.py (mit Import aus web/formatting.py) existiert als Standalone-Script.</done>
</task>

<task type="auto">
  <name>Task 2: Token-Rotations-Hinweis in .env.example dokumentieren</name>
  <files>.env.example</files>
  <read_first>.env.example</read_first>
  <action>
Am Anfang der Datei `.env.example` einen deutlichen Warnhinweis einfuegen, der darauf hinweist, dass die frueheren API-Tokens in der Git-History kompromittiert sind und bei ChurchDesk rotiert werden muessen.

Der Hinweis soll enthalten:
- WICHTIG-Label
- Erklaerung: Alte Tokens sind in der Git-History sichtbar (Commits vor Phase 1)
- Handlungsanweisung: Alle ChurchDesk API-Tokens muessen im ChurchDesk-Dashboard rotiert werden
- Die 5 betroffenen Organisationen: 2596, 6572, 2719, 2725, 2729
- Reihenfolge: Erst neue Tokens im ChurchDesk-Dashboard generieren, dann hier eintragen, dann Container neu starten

Format: Kommentarblock am Dateianfang (# ...), vor dem bestehenden Inhalt. Bestehenden Inhalt NICHT aendern.
  </action>
  <verify>
    <automated>grep -c "WICHTIG" /Users/simonluthe/Documents/kk-termine/.env.example</automated>
  </verify>
  <acceptance_criteria>.env.example enthaelt am Anfang einen Warnhinweis mit "WICHTIG" zur Token-Rotation. Alle bestehenden Env-Var-Definitionen sind unveraendert.</acceptance_criteria>
  <done>.env.example dokumentiert die Token-Rotations-Anforderung. Der Hinweis ist fuer jeden sichtbar, der die Datei als Vorlage verwendet.</done>
</task>

</tasks>

<verification>
1. `gottesdienst_formatter.py` existiert nicht mehr: `test ! -f gottesdienst_formatter.py`
2. `gottesdienst_formatter_final.py` existiert weiterhin: `test -f gottesdienst_formatter_final.py`
3. `.env.example` enthaelt Token-Rotations-Hinweis: `grep "WICHTIG" .env.example`
4. Keine duplizierte Formatierungslogik ausserhalb web/formatting.py: `grep -rn "def format_date\|def format_time\|def format_service_type\|def format_pastor" --include="*.py" . | grep -v web/formatting.py | grep -v __pycache__` — darf nur gottesdienst_formatter_final.py NICHT zeigen (da diese importiert statt definiert)
</verification>

<success_criteria>
- gottesdienst_formatter.py ist geloescht
- .env.example hat Token-Rotations-Hinweis
- Keine format_*-Definitionen ausserhalb von web/formatting.py (ausser in gottesdienst_formatter_final.py, die importiert)
- Beide Verifikations-Gaps (SEC-01 Git-History, CODE-01 Prototyp) sind geschlossen
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilisierung/01-03-SUMMARY.md`
</output>
