---
phase: 01-stabilisierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/formatting.py
  - web/config.py
  - web/app.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - CODE-01
  - CODE-02
  - CODE-03

must_haves:
  truths:
    - "Alle format_* Funktionen existieren genau einmal in web/formatting.py"
    - "format_pastor() in formatting.py basiert auf der vollstaendigen format_boyens_pastor()-Logik (Delimiter-Splitting, Dn., D., Ps., Praedikant)"
    - "web/app.py enthaelt keine eigenen format_date/format_time/format_service_type/format_pastor Definitionen mehr"
    - "Flask Secret Key kommt aus os.getenv('SECRET_KEY') mit secrets.token_hex(32) Fallback"
    - "web/config.py laedt Organisationen aus CHURCHDESK_ORG_IDS und CHURCHDESK_ORG_{ID}_TOKEN Env-Vars"
  artifacts:
    - path: "web/formatting.py"
      provides: "Zentrale Formatierungsfunktionen"
      exports: ["format_date", "format_time", "format_service_type", "format_pastor"]
    - path: "web/config.py"
      provides: "Env-Var-basierte Organisationskonfiguration"
      exports: ["load_organizations", "ORGANIZATIONS"]
  key_links:
    - from: "web/app.py"
      to: "web/formatting.py"
      via: "from formatting import format_date, format_time, format_service_type, format_pastor"
      pattern: "from formatting import"
    - from: "web/app.py"
      to: "web/config.py"
      via: "from config import ORGANIZATIONS"
      pattern: "from config import"
---

<objective>
Zentrale Module fuer Formatierung und Konfiguration erstellen, app.py bereinigen.

Purpose: Eliminiert die dreifache Formatierungslogik-Duplizierung und schafft die Grundlage fuer sichere, konfigurierbare Organisation-Verwaltung. Alle nachfolgenden Aenderungen an Formatierung oder Org-Konfiguration geschehen ab jetzt an genau einer Stelle.

Output: Zwei neue Module (formatting.py, config.py) und eine bereinigte app.py ohne duplizierte Logik und ohne hardcoded Secret Key.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md

<interfaces>
<!-- Aktuelle Exports aus web/churchdesk_api.py die app.py importiert (Zeile 16): -->
from churchdesk_api import ChurchDeskAPI, EventAnalyzer, create_multi_org_client, ORGANIZATIONS, extract_boyens_location, format_boyens_pastor

<!-- format_boyens_pastor Signatur (churchdesk_api.py Z. 358): -->
def format_boyens_pastor(contributor: str) -> str:

<!-- format_date/format_time/format_service_type/format_pastor Signaturen (app.py Z. 36-125): -->
def format_date(date_obj):      # nimmt pd.Timestamp oder datetime, nutzt pd.isna()
def format_time(date_obj):      # nimmt pd.Timestamp oder datetime, nutzt pd.isna()
def format_service_type(titel):  # nimmt str oder None, nutzt pd.isna()
def format_pastor(mitwirkender): # nimmt str oder None, nutzt pd.isna()

<!-- process_excel_file ruft alle vier format_* Funktionen auf (app.py Z. 127-169) -->
<!-- churchdesk_api.py ruft format_boyens_pastor intern auf (Z. 236 in EventAnalyzer) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/formatting.py erstellen — zentrale Formatierungsfunktionen</name>
  <files>web/formatting.py</files>
  <read_first>
    - web/app.py (Zeilen 36-125: format_date, format_time, format_service_type, format_pastor — die Excel-Pfad-Varianten)
    - web/churchdesk_api.py (Zeilen 358-422: format_boyens_pastor — die vollstaendige Pastor-Variante)
  </read_first>
  <action>
Neue Datei web/formatting.py erstellen mit folgenden Funktionen:

1. `format_date(date_obj)` — Kopie aus app.py Z. 36-56. Aenderung: statt `pd.isna(date_obj)` am Anfang pruefen mit `if date_obj is None or (isinstance(date_obj, float) and __import__('math').isnan(date_obj)): return ""`. ZUSAETZLICH `import pandas as pd` beibehalten und `pd.isna()` nutzen, da pandas sowieso Dependency ist. Entscheidung: pd.isna() beibehalten, weil das Modul von der Web-App genutzt wird die pandas hat.

2. `format_time(date_obj)` — Kopie aus app.py Z. 58-69. Gleiche isna-Logik.

3. `format_service_type(titel)` — Kopie aus app.py Z. 71-101 (die VOLLSTAENDIGERE Version mit Tauffest, Diamantene/Goldene/Silberne Konfirmation, Abendsegen, Kinderkirche, Familiengd., Andacht). Gleiche isna-Logik.

4. `format_pastor(contributor: str) -> str` — Basiert auf format_boyens_pastor() aus churchdesk_api.py Z. 358-422. Dies ist die KANONISCHE Implementierung mit:
   - Delimiter-Splitting (`, `, ` &amp; `, ` und `, ` + `, ` / `)
   - Sonderfaelle: Kirchspiel-Pastor:innen, Konfirmand:innen
   - Vollstaendige Prefix-Liste: Pastores, Pastor, Pastorin, Pfarrer, Diakon, Diakonin, Praedikant, Praedikantin, P., Pn., Ps., D., Dn.
   - Unbekannte Praefixe: Original beibehalten (NICHT erzwungenes "P.")
   - Rueckgabe mit ` &amp; ` verbunden
   - KEIN pd.isna()-Check noetig — Eingabe ist str, None-Check reicht: `if not contributor: return ""`

Datei-Header:
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Zentrale Formatierungsfunktionen fuer Boyens-Medien-Output.
Einzige Quelle der Wahrheit — alle Codepfade importieren von hier.
"""
import pandas as pd
```

WICHTIG: Die Funktion heisst `format_pastor` (nicht format_boyens_pastor). Der alte Name ist ein Implementierungsdetail.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && python -c "from web.formatting import format_date, format_time, format_service_type, format_pastor; print('OK:', format_pastor('Pastorin Schmidt, Diakon Mueller'))"</automated>
  </verify>
  <acceptance_criteria>
    - web/formatting.py existiert und enthaelt `def format_date(`
    - web/formatting.py enthaelt `def format_time(`
    - web/formatting.py enthaelt `def format_service_type(`
    - web/formatting.py enthaelt `def format_pastor(`
    - web/formatting.py enthaelt NICHT `def format_boyens_pastor(` (alter Name)
    - format_pastor enthaelt Delimiter-Splitting: `delimiters = [', ', ' &amp; ', ' und ', ' + ', ' / ']`
    - format_pastor enthaelt `'diakonin' in contrib_lower` Check
    - format_service_type enthaelt `'tauffest' in titel_lower` Check
    - format_service_type enthaelt `'diamantene konfirmation' in titel_lower` Check
  </acceptance_criteria>
  <done>web/formatting.py enthaelt alle vier format_*-Funktionen, format_pastor basiert auf der vollstaendigen format_boyens_pastor-Logik, Modul ist importierbar</done>
</task>

<task type="auto">
  <name>Task 2: web/config.py erstellen — Org-Konfiguration aus Env-Vars</name>
  <files>web/config.py</files>
  <read_first>
    - web/churchdesk_api.py (Zeilen 15-41: aktuelles ORGANIZATIONS dict mit allen 5 Orgs und ihren Feldern name/token/description)
  </read_first>
  <action>
Neue Datei web/config.py erstellen mit:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Konfiguration fuer ChurchDesk-Organisationen.
Laedt alle Credentials aus Environment Variables — keine hardcoded Tokens.
"""
import os


def load_organizations() -> dict:
    """Laedt Organisationskonfiguration aus Env-Vars.

    Erwartet:
        CHURCHDESK_ORG_IDS=2596,6572,2719,2725,2729
        CHURCHDESK_ORG_{ID}_NAME=Kirchspiel Heide
        CHURCHDESK_ORG_{ID}_TOKEN=abc123...
        CHURCHDESK_ORG_{ID}_DESCRIPTION=Heide (St.-Juergen...)  # optional
    """
    org_ids_str = os.getenv('CHURCHDESK_ORG_IDS', '')
    if not org_ids_str:
        return {}

    orgs = {}
    for org_id_str in org_ids_str.split(','):
        org_id = int(org_id_str.strip())
        prefix = f"CHURCHDESK_ORG_{org_id}_"
        name = os.getenv(f"{prefix}NAME")
        token = os.getenv(f"{prefix}TOKEN")
        if name and token:
            orgs[org_id] = {
                'name': name,
                'token': token,
                'description': os.getenv(f"{prefix}DESCRIPTION", '')
            }
    return orgs


ORGANIZATIONS = load_organizations()
```

Das ist die EXAKTE Implementierung wie im RESEARCH.md Pattern 2 beschrieben. Keine Abweichungen.

WICHTIG — KEIN hardcoded Fallback auf alte Tokens. Wenn CHURCHDESK_ORG_IDS nicht gesetzt ist, ist ORGANIZATIONS ein leeres Dict. Das ist beabsichtigt (per SEC-01).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && CHURCHDESK_ORG_IDS=9999 CHURCHDESK_ORG_9999_NAME=Test CHURCHDESK_ORG_9999_TOKEN=tok123 python -c "from web.config import ORGANIZATIONS; assert 9999 in ORGANIZATIONS and ORGANIZATIONS[9999]['token']=='tok123'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - web/config.py existiert und enthaelt `def load_organizations()`
    - web/config.py enthaelt `ORGANIZATIONS = load_organizations()`
    - web/config.py enthaelt `os.getenv('CHURCHDESK_ORG_IDS'`
    - web/config.py enthaelt KEINEN hardcoded Token-String (kein `d4ec66`, kein `7b0cf9`, kein `c2d76c`, kein `3afe57`, kein `bZq4GL`)
    - web/config.py enthaelt KEINEN `:-` Fallback-Syntax
  </acceptance_criteria>
  <done>web/config.py laedt Organisationen rein aus Env-Vars, keine hardcoded Credentials</done>
</task>

<task type="auto">
  <name>Task 3: web/app.py bereinigen — Imports umstellen, Duplikate entfernen, Secret Key fixen</name>
  <files>web/app.py</files>
  <read_first>
    - web/app.py (vollstaendig — alle Zeilen, um Imports, Funktionsdefinitionen und Aufrufstellen zu verstehen)
    - web/formatting.py (soeben erstellt — um korrekte Import-Namen zu kennen)
    - web/config.py (soeben erstellt — um ORGANIZATIONS Import zu kennen)
  </read_first>
  <action>
web/app.py aendern:

1. IMPORT-ZEILE 16 aendern von:
   `from churchdesk_api import ChurchDeskAPI, EventAnalyzer, create_multi_org_client, ORGANIZATIONS, extract_boyens_location, format_boyens_pastor`
   zu:
   `from churchdesk_api import ChurchDeskAPI, EventAnalyzer, create_multi_org_client, extract_boyens_location`
   `from formatting import format_date, format_time, format_service_type, format_pastor`
   `from config import ORGANIZATIONS`

2. NEUEN IMPORT hinzufuegen (nach `import os`):
   `import secrets`

3. SECRET KEY (Zeile 19) aendern von:
   `app.secret_key = 'gottesdienst-formatter-secret-key'`
   zu:
   `app.secret_key = os.getenv('SECRET_KEY') or secrets.token_hex(32)`

4. format_parish_name (Zeile 21-31) — BEIBEHALTEN, diese Funktion ruft extract_boyens_location auf, nicht format_*.

5. LOESCHEN: Die folgenden Funktionsdefinitionen komplett entfernen:
   - `def format_date(date_obj):` (aktuell Z. 36-56)
   - `def format_time(date_obj):` (aktuell Z. 58-69)
   - `def format_service_type(titel):` (aktuell Z. 71-101)
   - `def format_pastor(mitwirkender):` (aktuell Z. 103-125)

6. process_excel_file (aktuell Z. 127-169) — BEIBEHALTEN, die Funktion ruft format_date/format_time/format_service_type/format_pastor auf, die jetzt aus formatting.py kommen.

7. Alle Aufrufstellen die `format_boyens_pastor` referenzieren: Ersetzen durch `format_pastor` (neuer Name aus formatting.py). Suche in der gesamten Datei nach `format_boyens_pastor` und ersetze durch `format_pastor`.

8. Index-Route (aktuell Z. 171-173) aendern:
   Von: `return render_template('index.html')`
   Zu: `return render_template('index.html', organizations=ORGANIZATIONS)`
   Damit das Template die Org-Liste dynamisch rendern kann (wird in Plan 02 verdrahtet).

KEINE weiteren Aenderungen an Routes oder Templates in diesem Task.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine/web && python -c "import app; print('Flask app loads OK')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - web/app.py enthaelt `from formatting import format_date, format_time, format_service_type, format_pastor`
    - web/app.py enthaelt `from config import ORGANIZATIONS`
    - web/app.py enthaelt `app.secret_key = os.getenv('SECRET_KEY') or secrets.token_hex(32)`
    - web/app.py enthaelt `import secrets`
    - web/app.py enthaelt NICHT `app.secret_key = 'gottesdienst-formatter-secret-key'`
    - web/app.py enthaelt KEINE `def format_date(` Definition
    - web/app.py enthaelt KEINE `def format_time(` Definition
    - web/app.py enthaelt KEINE `def format_service_type(` Definition
    - web/app.py enthaelt KEINE `def format_pastor(` Definition (als eigene Funktion — Import ist OK)
    - web/app.py enthaelt `organizations=ORGANIZATIONS` im render_template-Aufruf der Index-Route
    - web/app.py enthaelt NICHT `format_boyens_pastor` (alter Funktionsname)
  </acceptance_criteria>
  <done>app.py importiert Formatierung aus formatting.py, Orgs aus config.py, Secret Key aus Env-Var, keine duplizierten Funktionen mehr</done>
</task>

</tasks>

<verification>
- `cd /Users/simonluthe/Documents/kk-termine/web && python -c "from formatting import format_date, format_time, format_service_type, format_pastor; print('formatting OK')"` exitiert mit 0
- `cd /Users/simonluthe/Documents/kk-termine/web && CHURCHDESK_ORG_IDS=1 CHURCHDESK_ORG_1_NAME=X CHURCHDESK_ORG_1_TOKEN=Y python -c "from config import ORGANIZATIONS; print('config OK')"` exitiert mit 0
- `grep -c 'def format_date\|def format_time\|def format_service_type\|def format_pastor' web/app.py` gibt 0 zurueck
- `grep -c 'def format_date\|def format_time\|def format_service_type\|def format_pastor' web/formatting.py` gibt 4 zurueck
- `grep -q "secret_key = 'gottesdienst" web/app.py` gibt Exit-Code 1 (nicht gefunden)
</verification>

<success_criteria>
1. web/formatting.py ist die einzige Datei mit format_date, format_time, format_service_type, format_pastor Definitionen
2. web/config.py laedt ORGANIZATIONS aus Env-Vars ohne hardcoded Tokens
3. web/app.py hat keine duplizierten Formatierungsfunktionen und keinen hardcoded Secret Key
4. Flask-App startet ohne ImportError
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilisierung/01-01-SUMMARY.md`
</output>
