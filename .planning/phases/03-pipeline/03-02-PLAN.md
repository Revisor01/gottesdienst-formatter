---
phase: 03-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .github/workflows/ci-cd.yml
  - web/docker-compose.prod.yml
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "Ein git push auf main triggert GitHub Actions, Tests laufen, Image wird gebaut und in ghcr.io gepusht"
    - "docker-compose.prod.yml nutzt image: ghcr.io/revisor01/gottesdienst-formatter:latest statt build: ."
    - "Watchtower-Service ist in docker-compose.prod.yml konfiguriert und pollt alle 5 Minuten"
  artifacts:
    - path: ".github/workflows/ci-cd.yml"
      provides: "GitHub Actions CI/CD Pipeline"
      min_lines: 40
      contains: "ghcr.io"
    - path: "web/docker-compose.prod.yml"
      provides: "Produktions-Compose mit Image-Pull und Watchtower"
      contains: "watchtower"
  key_links:
    - from: ".github/workflows/ci-cd.yml"
      to: "ghcr.io/revisor01/gottesdienst-formatter"
      via: "docker/build-push-action push to registry"
      pattern: "ghcr.io"
    - from: "web/docker-compose.prod.yml"
      to: "ghcr.io/revisor01/gottesdienst-formatter"
      via: "image: directive"
      pattern: "image:.*ghcr.io"
---

<objective>
GitHub Actions CI/CD-Pipeline und Produktions-Docker-Compose mit Watchtower einrichten.

Purpose: Vollautomatische Pipeline: git push → Tests → Image Build → Registry Push → Watchtower Auto-Deploy. Kein manueller SSH-Zugriff mehr noetig nach einmaliger Server-Einrichtung.
Output: Funktionierender CI/CD-Workflow und produktionsreife docker-compose.prod.yml.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pipeline/03-RESEARCH.md
@.planning/phases/03-pipeline/03-01-SUMMARY.md

@web/Dockerfile
@web/docker-compose.yml
@web/requirements-dev.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI/CD-Workflow und Produktions-Compose erstellen</name>
  <files>.github/workflows/ci-cd.yml, web/docker-compose.prod.yml</files>
  <read_first>web/Dockerfile, web/docker-compose.yml, .planning/phases/03-pipeline/03-RESEARCH.md</read_first>
  <action>
    1. Erstelle .github/workflows/ci-cd.yml mit zwei Jobs:

       **Job 1: test**
       - runs-on: ubuntu-latest
       - actions/checkout@v4
       - actions/setup-python@v5 mit python-version: '3.11'
       - pip install -r web/requirements-dev.txt UND pip install -r web/requirements.txt (pandas wird fuer Tests benoetigt)
       - pytest web/tests/ -v

       **Job 2: build-and-push**
       - needs: test
       - runs-on: ubuntu-latest
       - if: github.event_name == 'push' && github.ref == 'refs/heads/main'
       - permissions: contents: read, packages: write
       - actions/checkout@v4
       - Image-Name lowercase erzwingen (Pitfall 4 aus RESEARCH.md):
         ```yaml
         - name: Set lowercase image name
           run: echo "IMAGE_LC=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV
         ```
       - docker/login-action@v3 mit registry: ghcr.io, username: ${{ github.actor }}, password: ${{ secrets.GITHUB_TOKEN }}
       - docker/metadata-action@v5 mit images: ghcr.io/${{ env.IMAGE_LC }}, tags: type=raw,value=latest und type=sha
       - docker/build-push-action@v6 mit context: ./web, push: true, tags und labels aus metadata

       Trigger: push auf main, pull_request auf main.

    2. Erstelle web/docker-compose.prod.yml (NEUE Datei — web/docker-compose.yml bleibt fuer lokale Entwicklung mit build: .):

       ```yaml
       services:
         gottesdienst-formatter:
           image: ghcr.io/revisor01/gottesdienst-formatter:latest
           container_name: gottesdienst-formatter
           ports:
             - "5001:5000"
           volumes:
             - ./uploads:/app/uploads
           restart: unless-stopped
           environment:
             - FLASK_ENV=production
             - FLASK_DEBUG=false
             - SECRET_KEY=${SECRET_KEY}
             - CHURCHDESK_ORG_IDS=${CHURCHDESK_ORG_IDS}
             - CHURCHDESK_ORG_2596_NAME=${CHURCHDESK_ORG_2596_NAME}
             - CHURCHDESK_ORG_2596_TOKEN=${CHURCHDESK_ORG_2596_TOKEN}
             - CHURCHDESK_ORG_6572_NAME=${CHURCHDESK_ORG_6572_NAME}
             - CHURCHDESK_ORG_6572_TOKEN=${CHURCHDESK_ORG_6572_TOKEN}
             - CHURCHDESK_ORG_2719_NAME=${CHURCHDESK_ORG_2719_NAME}
             - CHURCHDESK_ORG_2719_TOKEN=${CHURCHDESK_ORG_2719_TOKEN}
             - CHURCHDESK_ORG_2725_NAME=${CHURCHDESK_ORG_2725_NAME}
             - CHURCHDESK_ORG_2725_TOKEN=${CHURCHDESK_ORG_2725_TOKEN}
             - CHURCHDESK_ORG_2729_NAME=${CHURCHDESK_ORG_2729_NAME}
             - CHURCHDESK_ORG_2729_TOKEN=${CHURCHDESK_ORG_2729_TOKEN}
             - CHURCHDESK_ORG_2596_DESCRIPTION=${CHURCHDESK_ORG_2596_DESCRIPTION:-}
             - CHURCHDESK_ORG_6572_DESCRIPTION=${CHURCHDESK_ORG_6572_DESCRIPTION:-}
             - CHURCHDESK_ORG_2719_DESCRIPTION=${CHURCHDESK_ORG_2719_DESCRIPTION:-}
             - CHURCHDESK_ORG_2725_DESCRIPTION=${CHURCHDESK_ORG_2725_DESCRIPTION:-}
             - CHURCHDESK_ORG_2729_DESCRIPTION=${CHURCHDESK_ORG_2729_DESCRIPTION:-}

         watchtower:
           image: containrrr/watchtower
           container_name: watchtower-gd
           volumes:
             - /var/run/docker.sock:/var/run/docker.sock
           environment:
             - WATCHTOWER_POLL_INTERVAL=300
             - WATCHTOWER_CLEANUP=true
             - WATCHTOWER_INCLUDE_STOPPED=false
           restart: unless-stopped
       ```

       WICHTIG: Kein `version: '3.8'` (deprecated in Compose V2). Env-Vars 1:1 aus bestehender docker-compose.yml uebernehmen.

    3. Validiere YAML-Syntax: `cd web && docker compose -f docker-compose.prod.yml config --quiet`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && test -f .github/workflows/ci-cd.yml && grep -q "ghcr.io" .github/workflows/ci-cd.yml && grep -q "pytest" .github/workflows/ci-cd.yml && cd web && docker compose -f docker-compose.prod.yml config --quiet 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci-cd.yml existiert mit test- und build-and-push-Jobs
    - Workflow triggert auf push/PR zu main
    - build-and-push hat needs: test und if-Bedingung fuer main-only
    - Image wird nach ghcr.io/revisor01/gottesdienst-formatter gepusht
    - docker-compose.prod.yml nutzt image: statt build: und enthaelt Watchtower-Service
    - YAML-Syntax valide
  </acceptance_criteria>
  <done>CI/CD-Workflow und Produktions-Compose erstellt, YAML-Syntax valide</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Server-Migration pruefen und ghcr.io-Paket freischalten</name>
  <what-built>
    Vollstaendige CI/CD-Pipeline: GitHub Actions Workflow (Test → Build → Push nach ghcr.io) und docker-compose.prod.yml mit Watchtower fuer Auto-Deploy.
  </what-built>
  <how-to-verify>
    **Schritt 1: GitHub Push und Actions pruefen**
    1. Aenderungen committen und pushen (wird vom Executor gemacht)
    2. Gehe zu https://github.com/Revisor01/gottesdienst-formatter/actions
    3. Pruefe ob der CI/CD-Workflow laeuft und erfolgreich ist
    4. Pruefe ob das Image unter https://github.com/Revisor01/gottesdienst-formatter/pkgs/container/gottesdienst-formatter erscheint

    **Schritt 2: ghcr.io Package oeffentlich machen (Pitfall 2)**
    1. Gehe zu https://github.com/users/Revisor01/packages/container/gottesdienst-formatter/settings
    2. Unter "Danger Zone" → "Change package visibility" → "Public" setzen
    3. Damit kann Watchtower ohne Credentials pullen

    **Schritt 3: Server einmalig umstellen**
    1. SSH auf den Server: `ssh root@185.248.143.234`
    2. `cd /opt/gottesdienst-formatter/web`
    3. Sicherung: `cp docker-compose.yml docker-compose.yml.bak`
    4. Neue Compose-Datei aktivieren: `cp docker-compose.prod.yml docker-compose.yml`
    5. Sicherstellen dass .env alle CHURCHDESK_ORG_* Variablen hat
    6. `docker compose pull && docker compose up -d`
    7. Pruefen: `docker ps` — gottesdienst-formatter und watchtower-gd muessen laufen
    8. Funktionstest: http://gd.kkd-fahrtenbuch.de muss erreichbar sein

    **Erwartetes Ergebnis:** Beide Container laufen, Webseite ist erreichbar, ab jetzt deployed jeder git push automatisch.
  </how-to-verify>
  <resume-signal>Antworten Sie mit "approved" wenn Pipeline und Server funktionieren, oder beschreiben Sie Probleme.</resume-signal>
</task>

</tasks>

<verification>
1. GitHub Actions Workflow laeuft erfolgreich (Test + Build + Push)
2. Docker Image ist auf ghcr.io verfuegbar
3. Server-Container laufen mit neuem Image
4. http://gd.kkd-fahrtenbuch.de ist erreichbar
</verification>

<success_criteria>
- git push auf main triggert CI/CD-Pipeline
- Tests laufen als Gate vor dem Image-Build
- Image wird automatisch nach ghcr.io/revisor01/gottesdienst-formatter:latest gepusht
- Watchtower pollt alle 5 Minuten und deployed neue Images automatisch
- Kein manueller SSH-Zugriff mehr fuer Deployments noetig
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline/03-02-SUMMARY.md`
</output>
