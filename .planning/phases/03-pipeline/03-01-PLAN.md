---
phase: 03-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tests/__init__.py
  - web/tests/conftest.py
  - web/tests/test_formatting.py
  - web/tests/fixtures/boyens_reference_input.json
  - web/tests/fixtures/boyens_reference_output.txt
  - web/tests/test_boyens_goldstandard.py
  - web/requirements-dev.txt
  - web/pyproject.toml
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "pytest web/tests/ laeuft durch und alle Tests sind gruen"
    - "Jede Formatierungsfunktion (format_date, format_time, format_service_type, format_pastor) hat mindestens 5 parametrisierte Testfaelle"
    - "Ein Goldstandard-Fixture vergleicht einen bekannten Input mit erwartetem Boyens-Output"
  artifacts:
    - path: "web/tests/test_formatting.py"
      provides: "Unit-Tests fuer alle Formatierungsfunktionen"
      min_lines: 80
    - path: "web/tests/test_boyens_goldstandard.py"
      provides: "End-to-End-Test mit Referenz-Fixture"
      min_lines: 20
    - path: "web/tests/conftest.py"
      provides: "sys.path-Fix und gemeinsame Fixtures"
    - path: "web/requirements-dev.txt"
      provides: "Test-Dependencies (pytest, pandas)"
    - path: "web/pyproject.toml"
      provides: "pytest-Konfiguration (pythonpath, testpaths)"
  key_links:
    - from: "web/tests/test_formatting.py"
      to: "web/formatting.py"
      via: "from formatting import format_date, format_time, format_service_type, format_pastor"
      pattern: "from formatting import"
    - from: "web/tests/test_boyens_goldstandard.py"
      to: "web/app.py"
      via: "from app import _build_location_entries, _extract_suffix"
      pattern: "_build_location_entries"
---

<objective>
Unit-Tests und Goldstandard-Fixture fuer alle Formatierungsfunktionen einrichten.

Purpose: Formatierungslogik absichern, damit Aenderungen nicht unbemerkt den Boyens-Output brechen. Grundlage fuer CI-Gate in Plan 02.
Output: Lauffaehige pytest-Testsuite mit parametrisierten Tests und Referenz-Fixture.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pipeline/03-RESEARCH.md

@web/formatting.py
@web/app.py
</context>

<interfaces>
<!-- Zu testende Funktionen aus web/formatting.py -->
```python
def format_date(date_obj) -> str:
    # Input: datetime oder pd.NaT, Output: "Sonnabend, 5. April"

def format_time(date_obj) -> str:
    # Input: datetime oder pd.NaT, Output: "9.30 Uhr" oder "17 Uhr"

def format_service_type(titel) -> str:
    # Input: str oder pd.NaT, Output: "Gd.", "Gd. m. A.", etc.

def format_pastor(contributor: str) -> str:
    # Input: str, Output: "P. Mueller", "Pn. Schmidt, P. Mueller", etc.
```

<!-- Hilfsfunktionen aus web/app.py (fuer Goldstandard-Test) -->
```python
def _extract_suffix(titel) -> str:
    # Extrahiert ", anschl. Kirchcafe" etc.

def _build_location_entries(day_items) -> list[str]:
    # Gruppiert nach Ort, wendet jeweils-Logik an
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pytest-Infrastruktur und Unit-Tests fuer formatting.py</name>
  <files>web/requirements-dev.txt, web/pyproject.toml, web/tests/__init__.py, web/tests/conftest.py, web/tests/test_formatting.py</files>
  <read_first>web/formatting.py, web/requirements.txt</read_first>
  <behavior>
    - format_date: datetime(2025,4,5) → "Sonnabend, 5. April", datetime(2025,4,6) → "Sonntag, 6. April", datetime(2025,12,25) → "Donnerstag, 25. Dezember", pd.NaT → ""
    - format_time: datetime(hour=9,minute=30) → "9.30 Uhr", datetime(hour=17,minute=0) → "17 Uhr", datetime(hour=10,minute=0) → "10 Uhr", pd.NaT → ""
    - format_service_type: "Gottesdienst" → "Gd.", "Abendmahlsgottesdienst" → "Gd. m. A.", "Taufgottesdienst" → "Gd. m. T.", "Gottesdienst mit Abendmahl und Taufe" → "Abendmahlgd. m. T.", "Konfirmation" → "Konfirmation", "Konfirmandenprüfung" → "Gd. m. Konfirmandenprüfung", "Familiengottesdienst" → "Familiengd.", "Gottesdienst mit Popularmusik" → "Gd. m. Popularmusik", "1. Konfirmation" → "1. Konfirmation", "Tauffest" → "Tauffest", "Diamantene Konfirmation" → "Diamantene Konfirmation", pd.NaT → "Gd."
    - format_pastor: "Pastor Mueller" → "P. Mueller", "Pastorin Schmidt" → "Pn. Schmidt", "Diakon Weber" → "Diakon Weber", "Diakonin Meier" → "Diakonin Meier", "Pastor Mueller & Pastorin Schmidt" → "P. Mueller, Pn. Schmidt", "Prädikantin Schulz" → "Prädikantin Schulz", "R. Bauer" → "R. Bauer", "" → "", "Pastor Soost & Team" → "P. Soost & Team"
  </behavior>
  <action>
    1. Erstelle web/requirements-dev.txt:
       ```
       pytest>=8.0
       pandas>=2.0
       openpyxl>=3.1
       ```

    2. Erstelle web/pyproject.toml mit pytest-Konfiguration:
       ```toml
       [tool.pytest.ini_options]
       pythonpath = ["."]
       testpaths = ["tests"]
       ```
       Damit loest sich das Import-Problem (Pitfall 5 aus RESEARCH.md): Tests koennen `from formatting import ...` direkt nutzen.

    3. Erstelle web/tests/__init__.py (leer).

    4. Erstelle web/tests/conftest.py mit sys.path-Fix als Backup:
       ```python
       import os, sys
       sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
       ```

    5. Erstelle web/tests/test_formatting.py mit @pytest.mark.parametrize fuer alle 4 Funktionen. Verwende die oben definierten Input/Output-Paare. WICHTIG:
       - NaT-Tests mit `import pandas as pd; pd.NaT` schreiben (Pitfall 1 aus RESEARCH.md)
       - Jede Funktion mindestens 5 Testfaelle
       - datetime-Objekte fuer format_date und format_time direkt erzeugen

    6. Installiere Test-Deps und fuehre Tests aus: `cd web && pip install -r requirements-dev.txt && pytest tests/ -v`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine/web && pip install -r requirements-dev.txt -q && pytest tests/test_formatting.py -v</automated>
  </verify>
  <acceptance_criteria>
    - web/tests/test_formatting.py existiert mit mindestens 20 parametrisierten Testfaellen
    - Alle Tests gruen bei `pytest tests/test_formatting.py -v`
    - Kein ModuleNotFoundError (pyproject.toml + conftest.py loesen Import)
  </acceptance_criteria>
  <done>Alle 4 Formatierungsfunktionen haben parametrisierte Unit-Tests, pytest laeuft fehlerfrei durch</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Goldstandard-Fixture und End-to-End-Formatierungstest</name>
  <files>web/tests/fixtures/boyens_reference_input.json, web/tests/fixtures/boyens_reference_output.txt, web/tests/test_boyens_goldstandard.py</files>
  <read_first>web/app.py (fuer _build_location_entries, _extract_suffix, convert_churchdesk_events_to_boyens), web/formatting.py</read_first>
  <behavior>
    - Gegebene statische Event-Daten (3-4 Termine an 2 Tagen) muessen exakt den erwarteten Boyens-Fliesstext produzieren
    - Test prueft: Datums-Ueberschrift mit Doppelpunkt, alphabetische Ortsortierung, korrekte Abkuerzungen, jeweils-Logik, Leerzeile nach jedem Tag
  </behavior>
  <action>
    1. Erstelle web/tests/fixtures/ Verzeichnis.

    2. Erstelle web/tests/fixtures/boyens_reference_input.json — statische Testdaten mit 4-5 Events an 2 verschiedenen Tagen:
       ```json
       [
         {
           "date": "2025-04-06",
           "time_hour": 10, "time_minute": 0,
           "location": "Heide, St.-Jürgen",
           "title": "Gottesdienst",
           "contributor": "Pastor Mueller"
         },
         {
           "date": "2025-04-06",
           "time_hour": 10, "time_minute": 0,
           "location": "Meldorf",
           "title": "Abendmahlsgottesdienst",
           "contributor": "Pastorin Schmidt"
         },
         {
           "date": "2025-04-06",
           "time_hour": 11, "time_minute": 0,
           "location": "Heide, St.-Jürgen",
           "title": "Kinderkirche",
           "contributor": "Pastorin Schmidt"
         },
         {
           "date": "2025-04-13",
           "time_hour": 9, "time_minute": 30,
           "location": "Wesselburen",
           "title": "Familiengottesdienst",
           "contributor": "Diakon Weber"
         }
       ]
       ```

    3. Erstelle web/tests/fixtures/boyens_reference_output.txt — den exakt erwarteten Output fuer obige Daten:
       ```
       Sonntag, 6. April:
       Heide, St.-Jürgen: 10 Uhr, Gd., P. Mueller; 11 Uhr, Kinderkirche, Pn. Schmidt
       Meldorf: 10 Uhr, Gd. m. A., Pn. Schmidt

       Sonntag, 13. April:
       Wesselburen: 9.30 Uhr, Familiengd., Diakon Weber

       ```

    4. Erstelle web/tests/test_boyens_goldstandard.py:
       - Lese Input-JSON und erwarteten Output
       - Baue day_items-Struktur aus den Input-Daten auf (nutze format_time, format_service_type, format_pastor aus formatting.py)
       - Rufe _build_location_entries() fuer jeden Tag auf
       - Vergleiche resultierenden Text Zeile fuer Zeile mit der Referenz
       - WICHTIG: _build_location_entries und _extract_suffix aus app.py importieren (mit sys.path-Fix aus conftest.py)

    5. Teste: `cd web && pytest tests/test_boyens_goldstandard.py -v`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine/web && pytest tests/test_boyens_goldstandard.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Fixture-Dateien existieren (boyens_reference_input.json, boyens_reference_output.txt)
    - Goldstandard-Test laeuft gruen
    - Gesamte Testsuite laeuft gruen: `pytest tests/ -v`
  </acceptance_criteria>
  <done>Goldstandard-Fixture existiert und wird als End-to-End-Test gegen die Formatierungspipeline geprueft</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/kk-termine/web && pytest tests/ -v --tb=short
Erwartung: Alle Tests gruen, keine Imports fehlgeschlagen, mindestens 20 Testfaelle insgesamt.
</verification>

<success_criteria>
- pytest tests/ laeuft komplett durch mit 0 Fehlern
- test_formatting.py hat mindestens 20 parametrisierte Testfaelle (5+ pro Funktion)
- test_boyens_goldstandard.py vergleicht statischen Input mit erwartetem Boyens-Output
- requirements-dev.txt und pyproject.toml konfiguriert
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline/03-01-SUMMARY.md`
</output>
