---
phase: 02-formatierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/formatting.py
autonomous: true
requirements:
  - FMT-02
  - FMT-03
  - FMT-04
  - FMT-05
  - FMT-09

must_haves:
  truths:
    - "format_service_type('Abendmahl und Taufe') returns 'Abendmahlgd. m. T.'"
    - "format_service_type('Gottesdienst mit Popularmusik') returns 'Gd. m. Popularmusik'"
    - "format_service_type('Konfirmandenprüfung') returns 'Gd. m. Konfirmandenprüfung'"
    - "format_service_type('Gemeinschaft') returns 'Gd. der Gemeinschaft'"
    - "format_pastor('Diakon Müller') returns 'Diakon Müller' (ausgeschrieben, nicht D.)"
    - "format_pastor('Prädikantin Meier') returns 'Prädikantin Meier' (mit -in)"
    - "format_pastor('Pastorin Christ, Pastorin Hoffmann') returns 'Pn. Christ, Pn. Hoffmann' (Komma, nicht &)"
    - "format_pastor('R. Sierk') returns 'R. Sierk'"
    - "format_time gibt '9.30 Uhr' fuer 9:30 und '17 Uhr' fuer volle Stunden zurueck (FMT-02)"
    - "format_date gibt 'Sonnabend, 5. April' zurueck — Sonnabend statt Samstag, kein fuehrendes Null (FMT-03)"
    - "Ein Benutzer der den formatierten Output sieht, erkennt korrekte Boyens-Abkuerzungen ohne redaktionelle Nacharbeit"
  artifacts:
    - path: "web/formatting.py"
      provides: "Alle Boyens-konformen Formatierungsfunktionen"
      contains: "Abendmahlgd. m. T."
  key_links:
    - from: "web/formatting.py"
      to: "web/app.py"
      via: "import format_service_type, format_pastor"
      pattern: "from formatting import"
---

<objective>
Alle Formatierungsfunktionen in web/formatting.py auf Boyens-Konformitaet bringen: Gottesdienst-Typ-Abkuerzungen vervollstaendigen, Pastor-Titel korrigieren, Trennzeichen fixen. Bestehende format_date() und format_time() gegen Boyens-Anforderungen verifizieren.

Purpose: format_service_type() und format_pastor() sind die zentralen Funktionen — wenn sie korrekt sind, produziert jeder Codepfad (Excel + ChurchDesk) den richtigen Output. format_date() und format_time() muessen ebenfalls die Boyens-Vorgaben erfuellen.
Output: Korrigierte web/formatting.py mit allen 12 Gottesdienst-Typen und allen 8 Pastor-Titel-Regeln, plus verifizierte Datums-/Zeitformatierung.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-formatierung/02-CONTEXT.md
@web/formatting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: format_service_type() vervollstaendigen</name>
  <files>web/formatting.py</files>
  <read_first>web/formatting.py, .planning/phases/02-formatierung/02-CONTEXT.md</read_first>
  <action>
Erweitere die if/elif-Kette in format_service_type() in web/formatting.py. Die Reihenfolge ist kritisch — spezifischere Matches muessen VOR allgemeineren stehen.

Neue/geaenderte Regeln (per D-01 bis D-12):

1. Per D-04: VOR dem bestehenden 'abendmahl'-Check einfuegen:
   `elif 'abendmahl' in titel_lower and 'taufe' in titel_lower:` → return 'Abendmahlgd. m. T.'

2. Per D-07: Neuer Check:
   `elif 'popularmusik' in titel_lower:` → return 'Gd. m. Popularmusik'

3. Per D-08: Neuer Check:
   `elif 'konfirmandenprüfung' in titel_lower or 'konfirmandenpruefung' in titel_lower:` → return 'Gd. m. Konfirmandenprüfung'
   WICHTIG: Muss VOR dem generischen 'konfirmation'-Check stehen.

4. Per D-11: Neuer Check:
   `elif 'gemeinschaft' in titel_lower:` → return 'Gd. der Gemeinschaft'

5. Per D-10: Sonderformate-Erkennung. Wenn titel Anfuehrungszeichen enthaelt (sowohl „ als auch "):
   Titel direkt zurueckgeben ohne Abkuerzung. Check: `if '„' in titel or '"' in titel or '»' in titel:`
   → return titel (den Originaltitel unveraendert zurueckgeben).
   Dieser Check sollte frueh in der Funktion stehen (nach isna-Check, vor den spezifischen Typ-Checks).

6. Per D-12: Zusatzinfos werden NICHT in format_service_type() behandelt — sie werden in der Output-Assembly in app.py angehaengt (Plan 02). Hier keine Aenderung noetig.

7. Per D-06: Konfirmation mit Nummer — wenn der Titel eine Zahl vor "Konfirmation" enthaelt (z.B. "1. Konfirmation"), diese beibehalten:
   Den bestehenden Konfirmation-Check erweitern: Wenn `re.search(r'(\d+)\.\s*konfirmation', titel_lower)` matched, return z.B. '1. Konfirmation'.
   Dafuer `import re` am Anfang der Datei hinzufuegen.

Vollstaendige Reihenfolge der if/elif-Kette (von oben nach unten):
1. Sonderformate mit Anfuehrungszeichen (D-10)
2. Tauffest (bestehend)
3. Diamantene/Goldene/Silberne Konfirmation (bestehend)
4. Konfirmandenprüfung (D-08) — VOR generischem Konfirmation
5. Nummerierte Konfirmation "N. Konfirmation" (D-06)
6. Konfirmation generisch (bestehend)
7. Abendmahl + Taufe kombiniert (D-04) — VOR einzelnem Abendmahl/Taufe
8. Abendmahl einzeln (bestehend, D-02)
9. Taufe einzeln (bestehend, D-03)
10. Popularmusik (D-07)
11. Gemeinschaft (D-11)
12. Abendsegen (bestehend)
13. Kinderkirche (bestehend, D-09)
14. Familie → Familiengd. (bestehend, D-05)
15. Andacht (bestehend)
16. Fallback → Gd. (D-01)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && python3 -c "
import sys; sys.path.insert(0, 'web')
from formatting import format_service_type
assert format_service_type('Gottesdienst') == 'Gd.', f'D-01 failed: {format_service_type(\"Gottesdienst\")}'
assert format_service_type('Abendmahl') == 'Gd. m. A.', f'D-02 failed'
assert format_service_type('Taufe') == 'Gd. m. T.', f'D-03 failed'
assert format_service_type('Abendmahl und Taufe') == 'Abendmahlgd. m. T.', f'D-04 failed: {format_service_type(\"Abendmahl und Taufe\")}'
assert format_service_type('Familiengottesdienst') == 'Familiengd.', f'D-05 failed'
assert format_service_type('1. Konfirmation') == '1. Konfirmation', f'D-06 failed: {format_service_type(\"1. Konfirmation\")}'
assert format_service_type('Gottesdienst mit Popularmusik') == 'Gd. m. Popularmusik', f'D-07 failed'
assert format_service_type('Konfirmandenprüfung') == 'Gd. m. Konfirmandenprüfung', f'D-08 failed'
assert format_service_type('Kinderkirche') == 'Kinderkirche', f'D-09 failed'
assert format_service_type('Gemeinschaft') == 'Gd. der Gemeinschaft', f'D-11 failed'
print('ALL format_service_type CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
- grep 'Abendmahlgd. m. T.' web/formatting.py liefert Treffer
- grep 'Gd. m. Popularmusik' web/formatting.py liefert Treffer
- grep 'Gd. m. Konfirmandenprüfung' web/formatting.py liefert Treffer
- grep 'Gd. der Gemeinschaft' web/formatting.py liefert Treffer
- grep 'import re' web/formatting.py liefert Treffer
- Alle 11 assert-Statements im verify-Skript bestehen
  </acceptance_criteria>
  <done>Alle Gottesdienst-Typ-Abkuerzungen aus D-01 bis D-11 werden korrekt zurueckgegeben, Reihenfolge ist spezifisch-vor-generisch.</done>
</task>

<task type="auto">
  <name>Task 2: format_pastor() auf Boyens-Standard bringen und format_date/format_time verifizieren</name>
  <files>web/formatting.py</files>
  <read_first>web/formatting.py (nach Task 1 Aenderungen), .planning/phases/02-formatierung/02-CONTEXT.md</read_first>
  <action>
Korrigiere format_pastor() in web/formatting.py. Folgende Aenderungen:

1. Per D-15: Diakon AUSGESCHRIEBEN, nicht "D.":
   - Zeile `formatted_contributors.append("D. {}".format(clean_name))` aendern zu:
     `formatted_contributors.append("Diakon {}".format(clean_name))`
   - Zeile `formatted_contributors.append("Dn. {}".format(clean_name))` aendern zu:
     `formatted_contributors.append("Diakonin {}".format(clean_name))`

2. Per D-16: Praedikantin mit -in:
   - Den Check `elif 'prädikant' in contrib_lower:` aufteilen in zwei:
     `elif 'prädikantin' in contrib_lower:` → `formatted_contributors.append("Prädikantin {}".format(clean_name))`
     `elif 'prädikant' in contrib_lower:` → `formatted_contributors.append("Prädikant {}".format(clean_name))`
   - "Prädikantin" muss VOR "Prädikant" geprueft werden (weil "prädikant" in "prädikantin" enthalten ist).

3. Per D-17: R. als Titel beibehalten:
   - Neuen Check hinzufuegen: `elif 'r.' in contrib_lower:` → `formatted_contributors.append("R. {}".format(clean_name))`
   - In der prefixes-Liste 'R. ' hinzufuegen zum Entfernen.

4. Per D-18: Trennzeichen von '&' auf ',' aendern:
   - Die letzte Zeile `return ' & '.join(formatted_contributors)` aendern zu:
     `return ', '.join(formatted_contributors)`

5. Per D-19: "& Team" beibehalten:
   - VOR dem Delimiter-Splitting: Pruefen ob der Name auf "& Team" endet.
     Wenn ja: "& Team" vom Namen abschneiden, merken, und nach dem Formatieren wieder anhaengen.
   - Konkret: `has_team = name.endswith('& Team') or name.endswith('&amp; Team')`
     Wenn ja: `name = name.replace('& Team', '').replace('&amp; Team', '').strip()`
     Am Ende nach dem join: `if has_team: result += ' & Team'`

6. Per D-20: "jeweils"-Logik:
   - Diese Logik gehoert NICHT in format_pastor() sondern in die Multi-Termin-Zusammenfassung in app.py (Plan 02).
   - Hier KEINE Aenderung noetig.

7. Delimiter-Handling verbessern:
   - Das aktuelle Splitting bricht bei "Pn. Christ, Pn. Hoffmann" — weil ', ' der erste Delimiter ist.
   - Loesung: Erst auf ' & ' und ' und ' und ' + ' und ' / ' splitten. NUR wenn keiner davon matched, auf ', ' splitten.
   - Dadurch wird "Pn. Christ, Pn. Hoffmann, P. Soost" korrekt als Komma-separierte Liste gesplittet (Fallback).

Zusaetzlich: format_date() und format_time() VERIFIZIEREN (per FMT-02, FMT-03):
- format_date() muss "Sonnabend" statt "Samstag" ausgeben (D-21), kein fuehrendes Null beim Tag (D-24).
- format_time() muss "9.30 Uhr" fuer 9:30 und "17 Uhr" fuer volle Stunden ausgeben (D-23).
- Diese Funktionen werden im verify-Schritt mitgetestet. Falls sie die Anforderungen nicht erfuellen, muessen sie angepasst werden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/kk-termine && python3 -c "
import sys; sys.path.insert(0, 'web')
from formatting import format_pastor, format_date, format_time
from datetime import datetime

# === format_pastor tests (D-13 to D-19) ===
assert format_pastor('Pastor Müller') == 'P. Müller', f'D-13 failed: {format_pastor(\"Pastor Müller\")}'
assert format_pastor('Pastorin Christ') == 'Pn. Christ', f'D-14 failed: {format_pastor(\"Pastorin Christ\")}'
assert format_pastor('Diakon Weber') == 'Diakon Weber', f'D-15 failed: {format_pastor(\"Diakon Weber\")}'
assert format_pastor('Prädikantin Meier') == 'Prädikantin Meier', f'D-16 failed: {format_pastor(\"Prädikantin Meier\")}'
assert format_pastor('R. Sierk') == 'R. Sierk', f'D-17 failed: {format_pastor(\"R. Sierk\")}'
result = format_pastor('Pastorin Christ, Pastorin Hoffmann, Pastor Soost')
assert result == 'Pn. Christ, Pn. Hoffmann, P. Soost', f'D-18 failed: {result}'
result2 = format_pastor('Pn. Sievers & Team')
assert '& Team' in result2 and 'Pn. Sievers' in result2, f'D-19 failed: {result2}'
result3 = format_pastor('Pastorin Christ & Pastorin Hoffmann')
assert result3 == 'Pn. Christ, Pn. Hoffmann', f'Multi-& failed: {result3}'
assert format_pastor('') == '', 'Empty failed'
assert format_pastor('Kirchspiel-Pastor:innen') == 'Kirchspiel-Pastor:innen', 'Special case failed'
print('ALL format_pastor CHECKS PASSED')

# === format_time tests (FMT-02, D-23) ===
dt_930 = datetime(2025, 4, 5, 9, 30)
result_t1 = format_time(dt_930)
assert result_t1 == '9.30 Uhr', f'FMT-02 failed for 9:30: {result_t1}'
dt_17 = datetime(2025, 4, 5, 17, 0)
result_t2 = format_time(dt_17)
assert result_t2 == '17 Uhr', f'FMT-02 failed for 17:00: {result_t2}'
dt_11 = datetime(2025, 4, 5, 11, 0)
result_t3 = format_time(dt_11)
assert result_t3 == '11 Uhr', f'FMT-02 failed for 11:00: {result_t3}'
print('ALL format_time CHECKS PASSED')

# === format_date tests (FMT-03, D-21, D-24) ===
dt_sat = datetime(2025, 4, 5, 9, 0)  # Saturday April 5
result_d1 = format_date(dt_sat)
assert 'Sonnabend' in result_d1, f'FMT-03 failed - expected Sonnabend: {result_d1}'
assert '5. April' in result_d1, f'FMT-03 failed - expected 5. April: {result_d1}'
assert '05.' not in result_d1, f'FMT-03 failed - leading zero found: {result_d1}'
dt_sun = datetime(2025, 4, 6, 9, 0)  # Sunday April 6
result_d2 = format_date(dt_sun)
assert 'Sonntag' in result_d2, f'FMT-03 failed for Sunday: {result_d2}'
assert '6. April' in result_d2, f'FMT-03 failed - expected 6. April: {result_d2}'
print('ALL format_date CHECKS PASSED')

print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
- grep "Diakon {}" web/formatting.py findet die ausgeschriebene Diakon-Formatierung (nicht D.)
- grep "Prädikantin {}" web/formatting.py findet die Praedikantin-Formatierung
- grep "', '.join" web/formatting.py findet den Komma-Separator (nicht & join)
- grep "& Team" web/formatting.py findet die Team-Sonderbehandlung
- grep "'R. '" web/formatting.py findet den R.-Titel-Check
- Alle format_pastor assert-Statements bestehen
- format_time(9:30) == '9.30 Uhr' und format_time(17:00) == '17 Uhr' (FMT-02)
- format_date(Samstag) enthaelt 'Sonnabend' und keinen fuehrenden Null (FMT-03)
  </acceptance_criteria>
  <done>format_pastor() gibt alle Titel gemaess D-13 bis D-19 korrekt aus. Trennzeichen ist Komma. & Team wird beibehalten. format_date() und format_time() erfuellen Boyens-Vorgaben (FMT-02, FMT-03).</done>
</task>

</tasks>

<verification>
Alle drei Formatierungsfunktionen (format_service_type, format_pastor, format_date/format_time) mit den verify-Skripten testen. Alle Assertions muessen bestehen.
</verification>

<success_criteria>
- format_service_type() erkennt alle 12+ Gottesdienst-Typen aus der Boyens-Referenz (D-01 bis D-11)
- format_pastor() formatiert alle Titel korrekt (D-13 bis D-19)
- Trennzeichen zwischen Pastoren ist Komma (D-18)
- Sonderformate mit Anfuehrungszeichen werden direkt uebernommen (D-10)
- format_time() gibt "9.30 Uhr" bzw. "17 Uhr" zurueck (FMT-02, D-23)
- format_date() gibt "Sonnabend, 5. April" zurueck (FMT-03, D-21, D-24)
- Keine Regression bei bestehenden Funktionen
</success_criteria>

<output>
After completion, create `.planning/phases/02-formatierung/02-01-SUMMARY.md`
</output>
